# Xcard 项目九期需求文档

版本：v0.9 确认版
日期：2026-07-21
来源：客服人工结算、人工 VIP 积分与卖卡订单聊天闭环需求
用途：用于约束九期开发范围、权限、账务一致性、消息展示、审计和验收标准。

## 1. 九期目标

九期围绕“卖卡订单直接进入客服聊天、订单卡片化、客服人工确定最终到账金额、VIP 积分完全人工发放、客服在聊天中完成或取消订单”展开。

当前系统存在以下问题：

1. 用户创建卖卡订单后，前端只把订单内容放进聊天输入框，用户还要再次点击发送。
2. 聊天中订单以普通文本展示，用户和客服无法快速识别订单状态与可执行操作。
3. 客服完成订单时直接使用创建订单时的预计结算金额，无法反映实际验卡后的成交汇率。
4. VIP 等级当前仍可由已完成订单金额自动推动，不符合人工审核积分的运营要求。
5. 完成、金额调整、VIP 积分和聊天状态不是一个完整的原子操作，存在漏记、重复操作和账务不一致风险。

九期核心目标：

1. 用户点击卖卡确认后，订单直接发送到专属客服聊天，不再生成待发送草稿。
2. 用户端和客服端均以结构化订单卡片展示订单。
3. 客服可直接在聊天订单卡片中完成或取消订单。
4. 完成订单时由客服手动确认实际到账金额，并可手动填写本次 VIP 积分。
5. VIP0 到 VIP5 完全按 VIP 积分流水累计值判断，不再按订单完成或成交额自动升级。
6. 保留历史已经产生的 VIP 积分，不重置、不回收、不批量降级。
7. 钱包、订单状态、VIP 积分、操作审计和聊天状态必须在同一事务中保持一致。

## 2. 已确认业务决策

1. VIP0、VIP1、VIP2、VIP3、VIP4、VIP5 全部由客服手动发放的积分驱动。
2. 订单完成本身不自动增加任何 VIP 积分。
3. 历史已有 VIP 积分继续有效。
4. 实际到账金额必须大于 `0`。
5. VIP 积分允许填写 `0`，表示本次订单不给积分。
6. 用户卖卡后订单必须直接发送给客服，不能停留在聊天输入框等待再次发送。
7. 聊天内必须渲染订单卡片，而不是只显示一段难以操作的普通文本。
8. 客服可从聊天订单卡片完成或取消订单。
9. 已完成、已取消、争议中的订单不能由客服再次修改金额或积分。
10. 管理员可以查看和操作全部订单；普通客服只可以操作分配给自己的客户订单。

## 3. 角色范围

| 角色 | 九期能力 |
| --- | --- |
| 普通用户 | 创建卖卡订单；在聊天中查看订单卡片、凭证、金额和实时状态；不能修改结算金额、VIP 积分或订单状态 |
| 分配客服 | 查看自己客户的订单卡片；对待处理或处理中的订单执行完成、取消、争议；完成时填写实际到账金额和 VIP 积分 |
| 其他客服 | 不得查看或操作未分配给自己的客户订单 |
| 管理员 | 查看和操作全部订单；查看完整金额、积分与审计记录 |

## 4. 用户卖卡提交流程

### 4.1 提交前

用户继续选择：

1. 礼品卡品牌。
2. 卡面币种。
3. 卡面金额。
4. 数量。
5. 卡片类型。
6. 处理速度。
7. 可选卡号、备注和凭证图片。

页面继续显示系统预计结算金额，但必须标注为“预计到账”，不能当作最终入账承诺。

### 4.2 点击确认

用户点击“创建订单并发送”后，后端应在一个请求中：

1. 校验用户、国家、卡片、币种和当前有效报价。
2. 根据服务端报价计算预计结算金额并保存快照。
3. 创建唯一卖卡订单。
4. 获取或创建该用户的专属客服会话。
5. 创建一条与订单永久关联的结构化订单消息。
6. 附加凭证图片引用，不重复上传同一文件。
7. 通过现有实时消息通道通知客服。
8. 返回订单和消息结果。

前端不得再使用 `pending-support-draft` 或 `pending-support-image` 生成待发送内容。

### 4.3 幂等性

1. 同一个 `clientRequestId` 重试时只能返回原订单。
2. 重试不能创建第二张订单卡片或重复系统通知。
3. 同一幂等键提交不同内容必须拒绝。
4. 网络超时后用户再次点击不能产生重复订单。

## 5. 结构化订单消息

### 5.1 消息数据

订单消息必须通过订单 ID 与真实订单关联，不能把普通文本作为唯一数据源。

消息至少提供：

1. 订单 ID 与订单号。
2. 卡片名称。
3. 卡面币种、单张面值和数量。
4. 预计到账金额。
5. 最终到账金额。
6. 钱包币种。
7. 卡片类型与处理速度。
8. 凭证图片。
9. 当前订单状态。
10. 创建时间与更新时间。
11. 本次人工 VIP 积分，未结算时为空。

### 5.2 用户端卡片

用户端订单卡片展示：

1. 订单号和状态。
2. 卡片名称、币种、面值和数量。
3. 预计到账金额。
4. 完成后显示最终到账金额和本次 VIP 积分。
5. 取消后显示取消原因。
6. 凭证图片可点击查看。

用户端不显示完成、取消、改金额或改积分按钮。

### 5.3 客服端卡片

客服端在用户聊天时间线中直接显示订单卡片。

待处理或处理中订单显示：

1. “开始处理”。
2. “完成订单”。
3. “取消订单”。
4. “标记争议”。

终态订单只显示结果，不再显示可变更操作。

## 6. 客服人工完成订单

### 6.1 完成弹窗

客服点击“完成订单”后弹出结算窗口，包含：

1. 订单号和用户。
2. 系统预计到账金额，只读。
3. 实际到账金额，默认带出预计值，必填且必须大于 `0`。
4. VIP 积分，默认 `0`，必须大于或等于 `0`。
5. 调整原因。当实际到账金额与预计值不一致时必填；一致时可选。
6. 二次确认按钮。

实际到账金额使用用户钱包币种，不允许客服在此切换币种。

### 6.2 原子完成

一次完成操作必须在同一数据库事务内：

1. 再次锁定并读取订单，防止两个客服重复完成。
2. 校验操作者权限和订单状态。
3. 保存最终到账数值和展示文本。
4. 保存人工 VIP 积分流水；积分为 `0` 时不创建正向积分流水，但审计仍记录 `0`。
5. 写入订单操作审计。
6. 将订单状态更新为已完成。
7. 让钱包余额按最终到账金额生效。
8. 更新订单消息的实时展示状态。
9. 发送用户通知。

任一步骤失败时全部回滚。

### 6.3 并发与重复操作

1. 第一次完成成功后，后续完成请求必须返回明确冲突。
2. 同一订单的人工 VIP 积分只能随首次完成写入一次。
3. 前端点击后立即禁用按钮并显示处理中。
4. 后端必须使用数据库锁或等价并发保护，不能只依赖前端禁用。

## 7. VIP 积分与等级规则

### 7.1 积分来源

九期后，VIP 等级只读取 `vip_point_ledger` 的有效积分总和。

允许来源：

1. 历史已经存在的积分流水。
2. 客服完成订单时手动填写的积分。
3. 管理员未来通过独立修正流程产生的调整流水，此项九期不实现。

禁止来源：

1. 订单状态自动完成奖励。
2. 已完成订单 USD 总额自动累计。
3. 前端传入但未经客服确认的预计成交额。

### 7.2 等级阈值

| 等级 | 积分条件 |
| --- | --- |
| VIP0 | `0` 积分 |
| VIP1 | 大于 `0` 且低于 `1000` |
| VIP2 | 大于或等于 `1000` |
| VIP3 | 大于或等于 `5000` |
| VIP4 | 大于或等于 `10000` |
| VIP5 | 大于或等于 `50000` |

订单完成但客服填写 `0` 积分时，VIP0 用户仍保持 VIP0。

### 7.3 历史兼容

1. 不删除历史积分流水。
2. 不把历史完成订单重新自动换算成积分。
3. 不因九期上线主动清零或降低已有积分。
4. 历史已完成订单保持原金额和状态。
5. 历史没有结构化订单消息时继续显示原文本消息，不批量补卡片。

## 8. 最终到账金额与钱包

1. 创建订单时保存预计到账金额。
2. 完成订单时新增并保存最终到账金额。
3. 钱包只将已完成订单的最终到账金额计入可用余额。
4. 待处理和处理中订单继续计入待结算金额，使用当前订单预计金额。
5. 取消或争议订单不计入可用余额。
6. 已完成订单金额对客服不可修改。
7. 九期不提供客服直接增加或扣减钱包余额的独立入口。
8. 后续如需修正已完成订单，必须设计管理员专用的正负调整流水，不能直接覆盖历史值。

## 9. 取消与争议

### 9.1 取消

1. 只有待处理或处理中订单可以取消。
2. 客服必须选择或填写取消原因。
3. 取消后订单卡片实时显示取消状态与原因。
4. 取消订单不增加钱包余额或 VIP 积分。
5. 重复取消必须拒绝。

### 9.2 争议

1. 待处理或处理中订单可标记为争议。
2. 争议订单不增加钱包余额或 VIP 积分。
3. 九期不增加争议后的重新打开流程。

## 10. 权限与安全

1. 普通用户不能调用人工完成、金额调整或积分发放接口。
2. 普通客服只能操作其专属会话下的客户订单。
3. 管理员可以操作全部订单。
4. 金额、积分和原因必须由后端校验。
5. 最终到账金额必须使用定点小数，禁止浮点误差。
6. VIP 积分必须使用定点小数且不得为负。
7. 接口不得信任前端传来的用户名、币种或订单归属。
8. 所有写操作必须验证订单当前状态和数据库最新版本。

## 11. 审计要求

每次人工完成至少记录：

1. 订单 ID。
2. 用户 ID。
3. 操作客服或管理员 ID。
4. 操作类型。
5. 原预计金额。
6. 最终到账金额。
7. 钱包币种。
8. 本次 VIP 积分。
9. 调整原因。
10. 操作时间。

审计记录创建后不得由客服编辑或删除。

## 12. 数据模型影响

### 12.1 订单表

建议为订单增加：

1. `estimated_local_amount`：创建时预计到账金额。
2. `final_local_amount`：完成时实际到账金额。
3. `manual_vip_points`：本次人工积分。
4. `settlement_reason`：调整原因。
5. `settled_by`：操作人。
6. `settled_at`：完成时间。

兼容策略：历史订单的现有 `local_amount` 视为历史结算金额；新订单创建时同时写预计值，完成时写最终值。

### 12.2 订单审计表

新增只追加的订单操作审计表，至少保存订单、操作者、动作、金额、积分、原因和时间。

### 12.3 支持消息表

订单消息增加可空的订单外键。普通历史消息保持为空，继续按原类型读取。

### 12.4 VIP 积分流水

复用现有 `vip_point_ledger`：

1. 人工订单积分使用唯一来源键，例如 `MANUAL_ORDER:<orderId>`。
2. 历史来源键保持不变。
3. 一个订单只能存在一个人工完成积分来源。

## 13. 接口影响建议

### 13.1 创建卖卡订单

`POST /api/transactions/sell-orders`

调整：

1. 默认直接创建关联订单消息。
2. 返回订单与消息关联结果，或保证消息可从会话同步接口立即读取。
3. 废弃前端 `sendChatMessage: false` 工作流。

### 13.2 人工完成订单

新增：

`POST /api/transactions/{orderId}/complete`

请求字段：

1. `finalLocalAmount`。
2. `vipPoints`。
3. `reason`。

普通状态接口不再允许直接把订单改为 `completed`，防止绕过人工金额和积分校验。

### 13.3 取消订单

继续使用取消接口，但订单卡片直接调用并刷新消息状态。

### 13.4 消息同步

聊天消息模型增加可选订单摘要。普通消息、图片、视频、语音和回复消息保持兼容。

## 14. 前端交互要求

### 14.1 用户卖卡页

1. 主按钮改为“创建订单并发送”。
2. 点击后显示提交中，禁止重复点击。
3. 成功后进入专属客服聊天，并定位到新订单卡片。
4. 不在输入框生成订单文本。
5. 凭证图片直接显示在订单卡片中。

### 14.2 用户聊天页

1. 订单卡片与普通聊天消息处于同一时间线。
2. 状态通过消息同步或订单刷新实时变化。
3. 用户刷新页面后仍能看到订单卡片。

### 14.3 客服聊天页

1. 新订单到达时显示未读提醒和订单卡片。
2. 操作按钮直接位于订单卡片。
3. 完成弹窗适配桌面、平板和手机。
4. 金额和积分输入使用明确单位，不允许误把钱包币种当 USD。
5. 操作成功后卡片立即变为终态并隐藏按钮。

## 15. 通知与实时刷新

1. 创建订单后通知分配客服和管理员。
2. 完成或取消后通知用户。
3. 用户聊天、客服聊天、订单列表、钱包和 VIP 页面刷新后必须一致。
4. WebSocket 消息只作为即时刷新信号，数据库仍是最终事实来源。
5. 推送失败不能回滚已经成功的订单事务，但必须可记录和重试。

## 16. 非功能要求

1. 完成订单接口必须具备并发保护。
2. 金额和积分更新必须事务化。
3. 订单卡片在 390px 手机宽度无横向溢出。
4. 桌面客服工作台不能因订单卡片扩展破坏客户列表和聊天区域。
5. 普通文本、图片、视频、语音、回复、未读和翻译功能不得回归。
6. 新迁移必须从生产 V34 增量升级，不修改 V1-V34。

## 17. 验收场景

### 场景 A：用户直接发送订单

1. 用户填写卖卡信息并点击创建。
2. 只生成一笔订单。
3. 直接进入聊天并看到订单卡片。
4. 输入框为空，不需要再次发送。
5. 客服实时看到同一订单卡片。

### 场景 B：客服按预计金额完成

1. 客服点击完成。
2. 实际金额默认等于预计金额。
3. VIP 积分填写 `0`。
4. 完成成功后钱包增加实际金额，用户 VIP 保持原值。

### 场景 C：客服调整实际金额并发积分

1. 客服填写与预计值不同的正数金额。
2. 填写正数 VIP 积分和原因。
3. 完成后钱包按实际金额增加。
4. VIP 总积分增加一次，等级按新总积分更新。
5. 审计记录保存原值、新值、积分、原因和操作者。

### 场景 D：未授权客服操作

其他客服完成或取消不属于自己的客户订单时返回无权限，订单、钱包和积分均不变化。

### 场景 E：并发重复完成

两个完成请求同时到达时只能有一个成功；钱包和积分只能增加一次。

### 场景 F：取消订单

客服从卡片取消订单并填写原因；用户实时看到取消状态，钱包和积分不增加。

### 场景 G：历史兼容

1. 历史 VIP 积分仍存在。
2. 历史文本订单消息正常展示。
3. 历史已完成订单金额不被重算。

## 18. 测试范围

### 18.1 后端

1. 创建订单同时创建关联消息。
2. 幂等重试不重复消息。
3. 完成时金额、积分、审计和状态原子提交。
4. 金额为 `0` 或负数拒绝。
5. 积分为负数拒绝，`0` 允许。
6. 非授权角色拒绝。
7. 并发完成仅一次成功。
8. 不再自动按订单金额增加 VIP 积分。
9. 历史积分总和继续参与等级。
10. 钱包按最终到账金额计算。

### 18.2 前端

1. 用户一键创建并进入聊天。
2. 无待发送草稿。
3. 用户与客服订单卡片一致。
4. 客服完成弹窗校验。
5. 完成、取消后的实时状态。
6. 桌面和 390px 手机适配。

### 18.3 回归

1. 普通文本、图片、视频、语音和回复消息。
2. 未读角标、自动刷新和翻译。
3. 订单列表、钱包、提现和客服客户资料。
4. VIP 抽奖、生日、红包、节日和贷款权限。
5. Flyway 从 V34 升级。

## 19. 明确不在九期范围

1. 客服修改已完成订单。
2. 客服独立增加或扣减钱包余额。
3. 客服发放负 VIP 积分。
4. 管理员已完成订单修正流水界面。
5. 用户撤回已提交订单。
6. 自动外汇或礼品卡报价接口。
7. 历史文本订单消息批量转换为卡片。

## 20. 推荐实施顺序

1. 增加订单结算、审计和消息关联迁移。
2. 将 VIP 等级来源切换为积分流水并停止自动积分。
3. 实现人工完成订单事务接口。
4. 创建卖卡订单时直接生成结构化消息。
5. 用户聊天渲染订单卡片。
6. 客服聊天渲染卡片、完成弹窗和取消操作。
7. 补齐并发、权限、钱包、积分、消息和回归测试。

## 21. 九期确认结论

本文件按以下最终决定实施：

1. 九期是当前八期之后的新一期。
2. 所有 VIP 等级均由人工积分驱动。
3. 历史积分保留。
4. 实际到账金额必须大于 `0`。
5. VIP 积分允许为 `0`。
6. 用户订单一键创建并直接发送给客服。
7. 用户端和客服端使用结构化订单卡片。
8. 客服可以从聊天卡片完成或取消订单。
9. 完成操作原子写入最终金额、积分、审计、状态和钱包影响。
10. 终态订单对客服不可修改。
