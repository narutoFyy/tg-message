# Xcard 项目十期需求文档

版本：v1.0 确认版
日期：2026-07-29
范围：邀请链客服继承、新用户抽奖审批、尼日利亚锁定注册奖励

## 1. 本期目标

1. 邀请关系中的新用户继承邀请人的专属客服，保持同一邀请链由同一客服持续服务。
2. 新用户注册后默认不能抽奖，必须由其专属客服或管理员人工允许后才能使用抽奖机会。
3. 新注册的尼日利亚用户获得管理员当时配置的 NGN 注册奖励；奖励先锁定，在首笔卖卡订单成功后解锁并允许提现。
4. 所有关键限制均由后端强制执行，前端负责展示状态和提供被授权的操作入口。

## 2. 上线边界

1. 本期规则只影响新迁移部署后注册的账号。
2. 历史用户、历史客服分配、历史邀请链和历史抽奖资格保持不变。
3. 不批量补发尼日利亚奖励，不批量锁定或解锁历史奖励。
4. 不因本期上线迁移现有客户，也不级联修改已有邀请链的客服。

## 3. 邀请链客服继承

### 3.1 分配规则

1. A 的专属客服为 S，A 邀请 B 时，B 继承 S。
2. B 邀请 C、C 邀请 D/E 时，C、D、E 分别继承其直接邀请人的当前客服，因此整条邀请链自然保持为 S。
3. 邀请继承判断必须在现有客服分配数据库锁内执行，避免并发注册产生重复会话或错误分配。
4. 邀请人没有客服、客服已停用或邀请会话无有效分配时，新用户回退到现有的最少客户数平均分配算法。
5. 邀请继承产生的会话使用独立的分配状态，便于后台识别和审计。

### 3.2 后续人工转移

1. 本期不实现邀请链级联转移。
2. 已注册的 B/C/D/E 不因 A 后续从 S 转给 T 而自动迁移。
3. 后续新邀请用户继承其直接邀请人在注册当时的当前客服。

## 4. 新用户抽奖审批

### 4.1 初始状态

1. 本期上线后注册的用户默认处于“等待客服允许抽奖”状态。
2. 等待审批时转盘及奖品正常展示，但抽奖按钮禁用。
3. 后端抽奖接口必须再次校验审批状态，禁止绕过前端直接抽奖。
4. 等待审批期间不生成或不开放新用户的 WELCOME 抽奖机会。

### 4.2 审批权限与审计

1. 该客户当前分配的客服可以允许其抽奖。
2. 管理员可以允许任意客户抽奖。
3. 其他客服不得操作不属于自己的客户。
4. 审批为一次性正向操作，审批后不提供撤销入口。
5. 系统记录审批状态、审批人和审批时间；重复审批返回已有状态，不重复创建抽奖机会。

### 4.3 用户与客服提示

用户等待审批：

`Lucky Wheel access is pending support approval. Please contact your assigned support agent.`

用户审批通过：

`Lucky Wheel access has been approved. You can now use your available draw chance.`

客服待处理：

`This customer is waiting for Lucky Wheel approval.`

## 5. 尼日利亚锁定注册奖励

### 5.1 奖励生成

1. 仅本期上线后新注册且国家绑定为 Nigeria (`NG`) 的用户获得本规则奖励。
2. 奖励金额读取注册瞬间管理员配置的 Nigeria/NGN 注册奖励，默认目标值为 `NGN 2,000.00`。
3. 金额直接按 NGN 保存，不经过美元汇率换算。
4. 管理员继续可以修改 Nigeria 注册奖励配置；修改只影响修改后的新注册用户，既有奖励记录保留其金额快照。
5. 其他国家继续按现有注册奖励配置和行为处理。

### 5.2 锁定与余额

1. 新尼日利亚用户的注册奖励初始状态为 `LOCKED`。
2. 锁定奖励在钱包中可见，但不计入 `Available balance`。
3. 提现后端只允许使用可提现余额，锁定奖励不得被提现接口计入。
4. 钱包单独显示：`Locked welcome bonus: NGN 2,000.00`。

用户锁定提示：

`Your NGN 2,000 welcome bonus is locked. Complete your first successful gift card trade with customer support to unlock it for withdrawal.`

### 5.3 自动解锁

1. 当客服或管理员把该用户第一笔有效卖卡订单完成为 `COMPLETED` 时，在同一数据库事务内解锁注册奖励。
2. 只有用户卖卡业务订单可触发；抽奖、提现、贷款、好友交易或取消订单不得触发。
3. 解锁记录包含解锁时间和触发订单 ID。
4. 同一奖励只能解锁一次；并发或重复完成请求不得重复增加余额。
5. 解锁后奖励状态为 `AVAILABLE`，并立即计入可提现余额。

用户解锁提示：

`Your NGN 2,000 welcome bonus is now unlocked and available for withdrawal.`

## 6. 前端要求

### 6.1 客服工作台

1. 客户资料区展示抽奖权限状态、审批人和审批时间。
2. 待审批时，负责客服或管理员看到 `Allow Lucky Wheel` 按钮。
3. 点击时防重复提交；成功后立即刷新客户资料。
4. 注册奖励区明确显示 `locked` 或 `available`，并显示解锁条件或触发订单。

### 6.2 转盘页面

1. 等待审批时抽奖按钮禁用，并显示等待客服批准的完整提示。
2. 审批后按现有抽奖次数和 VIP 周期规则运行。
3. 不修改转盘显示奖项和实际四档现金奖池规则。

### 6.3 钱包与提现页面

1. 可用余额不包含锁定奖励。
2. 钱包注册奖励区显示状态、金额和解锁条件。
3. 提现页显示的最大可提金额与后端计算一致。

### 6.4 管理员后台

1. 保留注册奖励国家配置能力。
2. Nigeria 配置默认更新为 NGN 2,000.00。
3. 页面提示配置只影响之后注册的新用户，已发放记录不追溯修改。

## 7. 数据与权限要求

1. 使用新的 Flyway 增量迁移，不修改已经应用的迁移文件。
2. 需要保存新规则生效标记，明确区分历史用户和本期新用户。
3. 抽奖审批与奖励解锁必须可审计。
4. 客服审批接口必须验证会话归属；管理员拥有全局权限。
5. 注册、客服分配、订单完成、奖励解锁和抽奖机会生成必须保持事务一致性。

## 8. 验收场景

1. 直接注册的新用户按平均规则分配客服。
2. A 邀请 B，B 继承 A 的有效客服；B 邀请 C 时 C 继续继承同一客服。
3. 邀请人没有有效客服时，新用户正常回退平均分配。
4. 并发注册时每个用户只有一条客服会话，邀请继承结果稳定。
5. 新用户审批前资格接口返回不可抽，直接调用抽奖接口也被拒绝。
6. 负责客服和管理员可以审批；无关客服被拒绝；重复审批不重复创建机会。
7. 历史用户不受审批门槛影响。
8. 新尼日利亚用户获得配置快照金额，初始为锁定且可用余额不增加。
9. 锁定奖励不能提现。
10. 第一笔卖卡订单成功后奖励解锁并计入可用余额；重复完成不重复解锁。
11. 取消、处理中及非卖卡业务不触发解锁。
12. 管理员修改 Nigeria 奖励后，旧用户金额不变，新用户使用新金额。
13. 后端完整测试、前端类型检查、H5 构建和生产部署健康检查全部通过。
