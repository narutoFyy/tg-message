# Xcard 项目七期需求文档

版本：v0.7 草案
日期：2026-07-12
来源：管理员端礼品卡维护流程反馈
用途：用于确认七期礼品卡目录、管理员卡片选择、品牌图标统一、旧数据兼容、接口影响和验收标准。

## 1. 七期目标

七期重点围绕“统一礼品卡目录、管理员快速选择卡片、品牌图标自动对应、保留自定义卡片能力”展开。

当前管理员在汇率管理页面新增或编辑汇率时，需要手动输入卡片名称。用户端再根据名称文字模糊匹配品牌图标。这种方式存在以下问题：

1. 同一种卡片可能出现不同写法，例如 `Apple(itunes)`、`Apple iTunes`、`iTunes`。
2. 管理员输入大小写、空格或别名后，系统可能无法匹配正确图标。
3. 管理员新增汇率时看不到卡片图片，难以快速确认选中的品牌。
4. 首页、卖卡页、订单页和管理员页可能各自处理卡片名称，后续容易出现显示不一致。
5. 完全禁止手动输入又会限制业务扩展，因此需要同时支持“预设选择”和“自定义输入”。

核心目标：

1. 建立首批 11 种常见礼品卡的统一预设目录。
2. 管理员新增或编辑汇率时，可通过带品牌图标的下拉框选择预设卡片。
3. 管理员仍可切换到手动输入模式，添加预设目录之外的卡片。
4. 选择预设卡片后，系统自动带入标准卡片名称和稳定卡片代码。
5. 全端使用同一套卡片代码与图标映射，避免仅依赖卡片名称模糊匹配。
6. 继续复用项目现有代码生成品牌图标，不新增或下载真实礼品卡封面图片。
7. 兼容现有汇率、订单和历史卡片名称，不破坏已有业务数据。

## 2. 角色范围

| 角色 | 七期能力 |
| --- | --- |
| 管理员 | 在汇率管理页面通过下拉框选择预设卡片并查看对应图标；也可以手动输入自定义卡片；新增、编辑、暂停、启用和删除汇率的原有权限保持不变 |
| 客服 | 在客户订单、卖卡信息和聊天业务资料中看到统一的卡片名称与对应图标；不新增卡片目录管理权限 |
| 普通用户 | 在首页、卖卡页、订单页等位置看到稳定一致的卡片名称与图标；仍只能选择管理员已启用汇率的卡片 |

## 3. 首批预设卡片目录

七期首批预设以下 11 种卡片。`cardCode` 是系统内部稳定标识，标准名称用于界面展示，别名用于兼容历史数据和管理员手动输入。

| cardCode | 标准名称 | 兼容别名示例 | 图标来源 |
| --- | --- | --- | --- |
| `APPLE_ITUNES` | Apple / iTunes | Apple、iTunes、Apple(itunes)、Apple iTunes | 复用 `art.ts` 的 Apple 图标 |
| `STEAM` | Steam | Steam Card、Steam Gift Card | 复用 `art.ts` 的 Steam 图标 |
| `RAZER_GOLD` | Razer Gold | Razer、RazerGold | 复用 `art.ts` 的 Razer 图标 |
| `XBOX` | Xbox | Xbox Card、Xbox Gift Card | 复用 `art.ts` 的 Xbox 图标 |
| `EBAY` | eBay | Ebay、eBay Card | 复用 `art.ts` 的 eBay 图标 |
| `SEPHORA` | Sephora | Sephora Card | 复用 `art.ts` 的 Sephora 图标 |
| `GOOGLE_PLAY` | Google Play | Google、Google Card、Google Play Card | 复用 `art.ts` 的 Google 图标 |
| `VANILLA` | Vanilla | Vanilla Card、Vanilla Gift Card | 复用 `art.ts` 的 Vanilla 图标 |
| `AMERICAN_EXPRESS` | American Express | Amex、American Express Card | 复用 `art.ts` 的 American Express 图标 |
| `ZELLE` | Zelle | Zelle Card | 复用 `art.ts` 的 Zelle 图标 |
| `CHIME` | Chime | Chime Card | 复用 `art.ts` 的 Chime 图标 |

目录规则：

1. `cardCode` 一经上线不得因展示名称调整而改变。
2. 标准名称可以后续优化，但历史汇率和订单仍通过 `cardCode` 识别同一品牌。
3. 卡片图标不保存为数据库中的 Base64 或完整 Data URI，只保存稳定卡片代码。
4. 前端根据 `cardCode` 从统一图标目录获取图片。
5. 别名匹配忽略大小写、首尾空格，以及常见的空格、短横线和括号差异。
6. 新增别名时不能让两个预设卡片命中同一个输入值。

## 4. 功能需求明细

### 4.1 管理员卡片选择模式

功能描述：
管理员进入“汇率管理”页面后，卡种字段默认使用带图标的预设卡片下拉框。管理员也可以切换为“手动输入”模式。

页面结构：

1. 卡种区域提供模式选择：
   - 预设卡片
   - 手动输入
2. 默认选择“预设卡片”。
3. 预设模式显示：
   - 当前卡片品牌图标
   - 标准卡片名称
   - 下拉箭头
4. 打开下拉框后，每个选项显示品牌图标和标准名称。
5. 手动输入模式显示文本输入框，并提示“输入预设目录之外的卡片名称”。
6. 地区、汇率、创建/更新按钮沿用现有流程。

交互规则：

1. 管理员选择预设卡片后，表单自动写入标准名称和 `cardCode`。
2. 管理员切换到手动输入时，不自动保留上一个预设卡片代码。
3. 手动输入内容如果能唯一匹配预设名称或别名，系统应自动归一为对应预设卡片。
4. 手动输入内容无法匹配预设目录时，按自定义卡片保存，`cardCode` 为空。
5. 自定义卡片在界面上使用统一默认卡片图标。
6. 管理员从手动输入切回预设模式时，应重新选择预设卡片，避免误提交旧值。
7. 编辑已有汇率时：
   - 能识别为预设卡片的，自动进入预设模式并选中对应项。
   - 不能识别的，自动进入手动输入模式并保留原卡名。
8. 提交前必须校验卡片名称、地区和汇率均不为空。
9. 管理员取消编辑或提交成功后，表单恢复默认状态。

验收标准：

1. 管理员新增汇率时可以看到卡片选择下拉框。
2. 下拉框完整展示 11 种预设卡片。
3. 每个预设选项都有正确品牌图标和名称。
4. 选择 Steam 后，表单保存 `STEAM` 和标准名称 `Steam`。
5. 切换到手动输入后，可以创建预设目录之外的卡片。
6. 手动输入未知卡片后，页面显示默认卡片图标，不错误套用其他品牌图标。
7. 编辑已有自定义卡片时，原始名称不会丢失。

### 4.2 下拉框搜索与移动端体验

功能描述：
预设卡片数量虽然首期只有 11 种，但下拉选择应具备清晰、可扩展的查找体验。

功能规则：

1. 桌面端可以使用下拉菜单或弹出选择面板。
2. 移动端可以使用底部选择面板，避免原生选择器无法展示图片。
3. 选择面板顶部提供搜索输入框。
4. 搜索范围包含标准名称和别名。
5. 搜索不区分大小写。
6. 没有结果时显示“未找到卡片，可切换到手动输入”。
7. 列表项高度固定，图标、名称和选中状态不能相互遮挡。
8. 已选卡片使用勾选图标标识，不只依赖颜色区分。
9. 关闭选择面板但未确认时，不改变原表单值。

验收标准：

1. 搜索 `itunes` 可以找到 Apple / iTunes。
2. 搜索 `amex` 可以找到 American Express。
3. 搜索不存在的名称时，可以直接进入手动输入模式。
4. 手机端列表、搜索框、关闭按钮和确认操作不重叠。

### 4.3 卡片图片统一规则

功能描述：
七期继续复用 `gift-chat-uni/src/utils/art.ts` 中现有的代码生成品牌图标，不下载、不生成新的真实礼品卡封面。

功能规则：

1. 建立统一的卡片目录模块，集中维护：
   - `cardCode`
   - 标准名称
   - 别名
   - 图标键
2. `cardLogoFor` 优先接收 `cardCode`，名称匹配只作为旧数据兼容方案。
3. 以下页面必须使用同一套图标解析逻辑：
   - 管理员汇率管理页
   - 普通用户首页汇率列表
   - 卖卡页面
   - 用户订单/交易记录页
   - 客服订单资料区
   - 管理员订单列表
4. 自定义卡片统一使用现有 fallback 图标。
5. 不允许每个页面各自复制一份品牌判断逻辑。
6. 图标加载失败时必须显示 fallback 图标，不能留下空白或破图。
7. 图标只用于品牌识别，不影响汇率计算、订单金额和业务状态。

验收标准：

1. 同一条 Steam 汇率在管理员页、首页和卖卡页显示同一个图标。
2. `Apple(itunes)` 历史数据和新建的 Apple / iTunes 汇率显示同一个图标。
3. 自定义名称 `Other Card` 在所有页面显示相同默认图标。
4. 页面刷新后图片映射保持不变。

### 4.4 卡片目录数据与接口

功能描述：
后端负责保存稳定的卡片代码和标准名称，前端负责根据代码展示内置图标。手动自定义卡片仍以名称保存。

建议数据调整：

1. `gift_card_rate` 增加可空字段 `card_code`：
   - 预设卡片保存对应稳定代码。
   - 自定义卡片保存 `NULL`。
2. 保留现有 `card_name` 字段：
   - 预设卡片保存标准名称。
   - 自定义卡片保存管理员输入并清理首尾空格后的名称。
3. 历史订单继续保留订单创建时的 `card_name` 快照，不能因目录名称调整而改写历史订单。
4. 如订单模型后续增加 `card_code`，该字段同样允许为空，不能阻塞旧订单读取。
5. 数据库变更必须使用新的 Flyway 迁移文件，不修改已经应用的旧迁移。

接口调整建议：

1. 汇率返回结构增加可空的 `cardCode`。
2. 新增/编辑汇率请求增加可空的 `cardCode`。
3. 选择预设卡片时，后端校验 `cardCode` 是否在允许目录中。
4. 传入有效 `cardCode` 时，后端以目录标准名称为准，不信任客户端伪造名称。
5. `cardCode` 为空时，按自定义卡片处理，并要求 `cardName` 非空。
6. 不允许普通用户通过接口新增或编辑卡片和汇率。
7. 卡片目录可以由前端静态内置；如后续需要后台维护目录，再单独升级为数据库目录接口。

验收标准：

1. 预设卡片汇率接口同时返回 `cardCode` 和标准名称。
2. 自定义卡片汇率返回 `cardCode: null` 和自定义名称。
3. 客户端提交不存在的 `cardCode` 时，后端拒绝保存。
4. 客户端提交 `STEAM` 配合伪造名称时，后端仍保存标准名称 `Steam`。
5. 普通用户不能调用管理员汇率维护接口。

### 4.5 历史数据兼容与归一化

功能描述：
现有数据库已经包含 Apple、Steam、Razer Gold、Zelle、Chime、Xbox、eBay、Sephora、Google、Vanilla、American Express 等名称。七期上线时需要识别这些历史记录，但不能破坏原订单。

迁移规则：

1. 对 `gift_card_rate` 中能够明确匹配的历史卡名回填 `card_code`。
2. 可明确匹配的汇率记录同步为标准展示名称。
3. 无法唯一匹配的记录不回填代码，原名称保持不变。
4. 历史订单、聊天消息和业务快照不批量改名。
5. 数据迁移必须可重复验证，不能因大小写差异漏掉明确别名。
6. 多条同品牌、同地区汇率暂不自动合并，避免误删业务配置。
7. 如果同一地区存在重复预设卡片汇率，管理员列表应保留现有记录，并提示运营人员人工处理。

建议回填示例：

| 现有名称 | 回填 cardCode | 标准名称 |
| --- | --- | --- |
| Apple(itunes) | `APPLE_ITUNES` | Apple / iTunes |
| Steam | `STEAM` | Steam |
| Razer Gold | `RAZER_GOLD` | Razer Gold |
| Google | `GOOGLE_PLAY` | Google Play |
| American Express | `AMERICAN_EXPRESS` | American Express |

验收标准：

1. 现有 11 种已知卡片均能匹配正确代码和图标。
2. 未知卡片名称不被错误转换。
3. 迁移前后的汇率数、状态和地区保持一致。
4. 历史订单内容不发生批量变化。

### 4.6 全端展示要求

管理员汇率管理页：

1. 新增/编辑表单显示卡片模式、卡片图标和卡片选择入口。
2. 汇率列表每行显示图标、标准名称、地区、汇率、状态和更新时间。
3. 自定义卡片增加“自定义”标识，帮助管理员识别未进入预设目录的卡片。

普通用户首页：

1. 汇率列表继续显示品牌图标和卡片名称。
2. 卡片筛选项按标准名称去重。
3. 同一品牌不同地区的图标保持一致。

卖卡页：

1. 从汇率记录进入卖卡页时，自动带入卡片代码、名称和图标。
2. 卡片选择列表只展示当前地区具有有效汇率的卡片。
3. 自定义卡片可以正常提交，不因缺少代码而报错。

订单与客服资料：

1. 新订单保留提交时的卡片名称快照。
2. 有 `cardCode` 时显示对应品牌图标。
3. 无 `cardCode` 的旧订单或自定义订单使用名称兼容匹配，仍无法匹配时使用 fallback。

### 4.7 权限、校验与异常处理

1. 只有管理员可以新增、编辑、暂停、启用或删除汇率记录。
2. 前端下拉框不是安全边界，后端必须再次校验预设代码。
3. 手动输入名称去除首尾空格后不能为空。
4. 手动输入名称长度沿用后端字段限制，超过限制时返回明确中文错误。
5. 不允许提交只有空格、控制字符或不可见字符的卡片名称。
6. 删除汇率不能删除预设卡片图标或影响其他地区同品牌汇率。
7. 图标解析异常不能阻塞汇率列表和卖卡业务。
8. 卡片目录加载失败时，管理员仍可看到错误提示，不自动切换成可能误提交的空表单。

## 5. 数据与接口影响汇总

| 范围 | 七期建议变更 |
| --- | --- |
| 数据库 | `gift_card_rate` 增加可空 `card_code`；已知历史汇率回填代码 |
| 后端实体 | `GiftCardRateEntity` 增加 `cardCode` |
| 后端模型 | `RateItem` 增加可空 `cardCode` |
| 后端请求 | 创建/编辑汇率请求增加可空 `cardCode` |
| 后端服务 | 校验预设代码、标准化卡名、兼容自定义卡片 |
| 前端类型 | `RateItem` 增加可空 `cardCode` |
| 前端目录 | 集中维护 11 种卡片代码、名称、别名和图标键 |
| 管理员页面 | 文本输入改为“预设下拉 + 手动输入”双模式 |
| 用户页面 | 首页、卖卡页、订单页统一按代码解析图标 |
| 测试 | 增加目录匹配、权限、迁移、预设选择和自定义卡片测试 |

## 6. 非功能要求

### 6.1 一致性

1. 所有页面必须通过同一个卡片目录工具解析名称和图标。
2. 后端保存的预设名称必须由服务端目录决定。
3. 自定义卡片不能因为名称中偶然包含某个短词而错误匹配品牌。

### 6.2 性能

1. 11 种预设卡片使用本地内置图标，不发生额外网络请求。
2. 打开下拉框和搜索过滤应即时响应。
3. 卡片目录不应为每条汇率重复生成不必要的大对象。

### 6.3 可维护性

1. 新增预设品牌时，只需在统一目录增加一项，并补充对应图标和测试。
2. 卡片代码、别名和标准名称不能散落在多个页面。
3. 默认图标必须长期保留，以兼容自定义和旧数据。

### 6.4 视觉要求

1. 管理员端继续沿用现有紧凑、清晰的后台布局。
2. 下拉列表图标大小统一，不因不同 SVG 比例导致行高变化。
3. 卡片名称过长时允许换行或省略，但不得覆盖状态和操作按钮。
4. 不使用嵌套卡片或过度装饰，重点保证选择效率和信息识别。

## 7. 完整验收场景

### 场景 A：管理员选择预设卡片

1. 管理员进入汇率管理页。
2. 打开卡片下拉框并搜索 `steam`。
3. 选择 Steam，页面显示 Steam 图标和标准名称。
4. 选择地区并填写汇率后提交。
5. 保存结果包含 `cardCode: STEAM`。
6. 用户首页和卖卡页显示相同 Steam 图标。

### 场景 B：管理员手动添加自定义卡片

1. 管理员切换到手动输入。
2. 输入 `Other Card`，填写地区和汇率后提交。
3. 保存结果的 `cardCode` 为空，名称为 `Other Card`。
4. 管理员页和用户页均显示默认卡片图标。
5. 用户仍可以使用该有效汇率创建卖卡订单。

### 场景 C：手动输入已知别名

1. 管理员手动输入 `Amex`。
2. 系统识别为 American Express 并向管理员显示归一化提示。
3. 提交后保存 `AMERICAN_EXPRESS` 和标准名称 `American Express`。
4. 各页面显示 American Express 图标。

### 场景 D：编辑历史汇率

1. 管理员编辑历史记录 `Apple(itunes)`。
2. 系统识别并选中 Apple / iTunes 预设项。
3. 管理员只修改汇率后保存。
4. 记录保存稳定代码和标准名称，地区及状态不变。
5. 历史订单中的旧名称保持原样。

### 场景 E：伪造卡片代码

1. 使用管理员身份提交不存在的 `cardCode`。
2. 后端拒绝请求并返回明确错误。
3. 使用普通用户身份调用汇率维护接口。
4. 后端返回无权限，不产生任何数据。

## 8. 测试要求

后端测试：

1. 11 种合法 `cardCode` 均可创建汇率。
2. 非法 `cardCode` 被拒绝。
3. 预设代码与伪造名称同时提交时，以服务端标准名称为准。
4. 自定义卡片允许 `cardCode` 为空，但名称不能为空。
5. 普通用户和客服不能维护汇率。
6. 历史已知名称正确回填代码，未知名称保持不变。
7. 现有汇率状态切换和删除功能不回归。

前端测试：

1. 管理员下拉框展示 11 种卡片及图标。
2. 搜索名称和别名均可找到正确卡片。
3. 预设模式和手动输入模式切换正确。
4. 编辑预设卡片和自定义卡片时模式回显正确。
5. 首页、卖卡页、订单页和管理员页使用相同图标。
6. 未知卡片稳定显示 fallback 图标。
7. H5、手机宽度和桌面宽度下没有文本或控件重叠。

建议验证命令：

```powershell
cd W:\cyproject\10-tg-message\tg-message\gift-chat-server
mvn test
```

```powershell
cd W:\cyproject\10-tg-message\tg-message\gift-chat-uni
npm run type-check
npm run build:h5
```

## 9. 七期实施任务拆分建议

1. 建立统一的前端卡片目录和 11 种品牌图标映射。
2. 增加 `card_code` 数据库字段并迁移明确可识别的历史汇率。
3. 更新后端实体、请求、返回模型和卡片代码校验。
4. 改造管理员汇率页为预设下拉与手动输入双模式。
5. 同步首页、卖卡页、订单页、客服资料和管理员订单图标展示。
6. 增加后端集成测试、前端类型检查、构建和多端人工验收。

## 10. 明确不在七期范围

1. 不下载或生成真实礼品卡封面图片。
2. 不允许管理员上传自定义品牌图片。
3. 不建设独立的后台卡片目录增删改页面。
4. 不自动合并或删除现有重复汇率。
5. 不批量修改历史订单、聊天消息或业务快照中的卡片名称。
6. 不改变现有汇率计算、订单结算、提现或抽奖规则。
7. 不处理品牌图标的商标授权和法律审核，该事项如需上线审查应单独确认。

## 11. 后续扩展方向

以下能力本期仅预留，不纳入七期验收：

1. 管理员维护预设卡片目录和别名。
2. 上传自定义品牌图片或礼品卡封面。
3. 按国家、币种或卡片类型限制可用品牌。
4. 卡片目录排序、热门品牌置顶和运营推荐。
5. 同一品牌支持不同产品系列或面额专属图片。
6. 卡片图标 CDN 化和后台版本管理。

---

本文件用于确认 Xcard 项目七期需求范围。功能开发应在本需求确认后单独进入实施阶段，任何新增图片上传、卡片目录后台管理或历史订单批量修改需求均需重新评估范围。
