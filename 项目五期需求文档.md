# CardBrother 项目五期需求文档

版本：v0.5 草案
来源：2026-07-06 客户新增需求
用途：用于确认五期开发范围、会员等级规则、抽奖项目集成、注册国家码选择、订单页中奖滚动展示和验收标准。

## 1. 五期目标

五期重点围绕“交易等级成长、抽奖激励、注册手机号国家码选择、中奖氛围展示”继续增强。当前目标是让用户通过交易获得积分并提升 VIP 等级，不同 VIP 等级拥有不同抽奖频率；同时将现有抽奖项目 `W:\cyproject\7-choujiang\choujiang` 的转盘、中奖记录滚动窗和奖品能力集成进 CardBrother 用户端。

核心目标：

1. 建立交易积分和 VIP 等级体系，用户完成交易后获得积分。
2. 在个人页展示当前 VIP 等级、积分和升级进度条。
3. 集成现有抽奖项目的转盘和中奖滚动名单，使用 CardBrother 当前用户体系，不再要求用户进入独立抽奖项目重新注册登录。
4. 按 VIP 等级控制抽奖资格和抽奖频率。
5. 注册页面手机号国家码改成下拉选择，避免用户手动输入 `+xx`。
6. 用户端订单页面顶部新增中奖记录滚动板块，沿用抽奖项目的滚动窗效果。
7. 客服端增强搜索和客户组织能力：支持搜索用户、搜索聊天记录，并按国家码自动生成客户分组。
8. 用户端支持删除/隐藏自己的聊天记录和订单记录；删除后仅用户自己不可见，服务器、客服端和管理员端仍保留完整记录。

## 2. 角色范围

| 角色 | 五期新增能力 |
| --- | --- |
| 普通用户 | 查看自己的 VIP 等级、积分和升级进度；符合等级规则时参与抽奖；在订单页看到中奖记录滚动窗；注册时从下拉框选择手机号国家码；可在自己端隐藏聊天记录和订单记录 |
| 客服 | 可在客户资料中查看客户 VIP 等级、积分和抽奖资格；在搜索栏中搜索用户和聊天记录；按国家码分组查看自己负责的客户；仍可查看用户已隐藏的完整订单和聊天记录 |
| 管理员 | 配置或查看 VIP 等级规则、抽奖奖品、中奖记录、用户抽奖次数和抽奖资格；仍可查看用户已隐藏的完整订单和聊天记录 |

## 3. 功能需求明细

### 3.1 交易积分与 VIP 等级制

功能描述：
系统根据用户已完成交易累计积分，并按积分门槛计算 VIP 等级。用户在个人页可以看到当前等级和到下一等级的进度。

初始等级规则：

| 等级 | 达成条件 | 积分规则 | 抽奖权益 |
| --- | --- | --- | --- |
| VIP1 | 注册后默认等级，或积分低于 20 | 交易 1 美刀获得 1 积分 | 新用户可抽奖，暂定注册后/首次进入抽奖页可抽 1 次 |
| VIP2 | 累计 20 积分 | 交易 1 美刀获得 1 积分 | 每周可抽 1 次 |
| VIP3 | 累计 100 积分 | 交易 1 美刀获得 1 积分 | 每天可抽 1 次 |
| VIP4 | 累计 500 积分 | 交易 1 美刀获得 1 积分 | 暂定不低于 VIP3，每天可抽 1 次；后续可扩展更高权益 |

功能规则：

1. 只有状态为“已完成”的交易计入积分。
2. 交易积分按美元金额计算：交易 1 美刀 = 1 积分。
3. 如果订单金额不是美元，需要系统有统一折算口径；五期先建议优先使用卖卡订单里的美元面值字段作为积分来源。
4. 订单取消、争议、处理中、待处理不计入积分。
5. 同一订单只能计一次积分，重复标记完成不能重复加积分。
6. 如果已完成订单后续发生回滚或取消，需要扣回对应积分；具体回滚规则待确认。
7. VIP 等级根据累计有效积分自动计算，不需要用户手动领取。
8. 个人页下方增加等级进度条：
   - 当前等级，例如 `VIP1`。
   - 当前积分，例如 `12 / 20`。
   - 距离下一等级还差多少积分。
   - 满级 VIP4 时显示已达最高等级。

验收标准：

1. 新注册用户默认显示 VIP1。
2. 用户完成 1 美刀交易后获得 1 积分。
3. 用户累计 20 积分后显示 VIP2。
4. 用户累计 100 积分后显示 VIP3。
5. 用户累计 500 积分后显示 VIP4。
6. 个人页可看到等级进度条，进度和下一等级门槛正确。
7. 未完成、已取消、争议订单不增加积分。

### 3.2 抽奖项目集成

功能描述：
将现有抽奖项目 `W:\cyproject\7-choujiang\choujiang` 集成进当前 CardBrother 项目。抽奖项目已有能力包括：转盘抽奖、奖品池、中奖者滚动名单、管理员查看用户和重置抽奖状态。五期目标不是让用户跳到独立 Flask 项目重新登录，而是把其核心能力迁移或嵌入到当前 Spring Boot + uni-app 体系中。

现有抽奖项目参考能力：

| 来源能力 | 当前抽奖项目位置 | 五期集成建议 |
| --- | --- | --- |
| 转盘页面 | `index.html` | 迁移为 uni-app 用户页或独立抽奖页组件 |
| 抽奖接口 | `/api/spin` | 改为 CardBrother 后端接口，使用当前登录用户 |
| 中奖列表 | `/api/winners` + `users.json` | 迁移为后端中奖记录表和用户端滚动接口 |
| 奖品池 | `PRIZE_POOL` | 迁移为数据库配置或后端常量，管理员后续可配置 |
| 管理员重置 | `/api/admin/reset-user` | 融入当前管理员后台，支持重置用户抽奖资格 |

功能规则：

1. 用户必须登录 CardBrother 后才能抽奖。
2. 抽奖资格由 VIP 等级和上次抽奖时间决定。
3. 抽奖结果由后端生成，前端只负责展示转盘动画，不能由前端传入最终奖品。
4. 每次抽奖必须保存记录：用户、VIP 等级、奖品、抽奖时间、抽奖周期类型、发放状态。
5. 奖品池现金展示位改为小额奈拉奖项，转盘保留 7 个现金位 + 3 个实物展示位：
   - `₦100`
   - `₦200`
   - `₦500`
   - `₦1000`
   - `₦2000`
   - `₦3000`
   - `₦5000`
   - `iPad`
   - `iPhone 17`
   - `Computer`
6. 实际可中奖现金奖项限制为 `₦1000` 以内，即只允许后端抽中 `₦100`、`₦200`、`₦500`、`₦1000`；`₦2000`、`₦3000`、`₦5000` 和实物奖品仅作为转盘展示位，后续可按运营策略再开放。
7. 中奖概率沿用原现金可抽奖项的权重顺序，分别为 `₦100` 权重 6、`₦200` 权重 7、`₦500` 权重 10、`₦1000` 权重 10。
8. 奖品图片可复用抽奖项目中的 `iphone.jpg`、`ipad.jpg`、`diannao.jpg`、`gsmz.jpg` 等素材，但需要复制到当前前端静态资源目录或通过后端统一静态资源访问。
9. 抽奖结果如果是现金类奖励，是否直接进入余额暂不确定，五期先要求记录中奖结果和发放状态。
10. 管理员可查看中奖记录，并可将奖品标记为已处理/已发放。

验收标准：

1. 用户进入抽奖页时，系统能显示是否有抽奖资格。
2. 有资格用户点击转盘后，后端返回一个中奖结果。
3. 用户抽奖后，中奖记录被保存。
4. 不符合频率限制的用户再次点击抽奖时，系统提示下次可抽奖时间。
5. 管理员能看到用户中奖记录。
6. 转盘动画和奖品展示能在 H5 端正常显示。
7. 后端抽奖结果只能落在 `₦100`、`₦200`、`₦500`、`₦1000` 四个 `₦1000` 以内现金奖项。
8. 前端转盘展示的小额现金位与后端奖品池一致，不再展示 `₦8000`、`₦10000`、`₦15000`。

### 3.3 VIP 抽奖频率规则

功能描述：
不同 VIP 等级对应不同抽奖频率，系统需要自动判断用户当前是否可抽奖。

抽奖频率规则：

1. VIP1：
   - 新用户进来可以抽奖。
   - 暂定为“每个账号 1 次新手抽奖”。
   - 抽过后，除非升级到 VIP2 或管理员重置，否则不能重复抽。
2. VIP2：
   - 每周可抽 1 次。
   - 每周周期建议按自然周计算，周一 00:00 到周日 23:59。
3. VIP3：
   - 每天可抽 1 次。
   - 每天周期建议按服务器时区自然日计算。
4. VIP4：
   - 暂定每天可抽 1 次。
   - 后续可增加更高中奖概率、额外次数或专属奖池。

功能规则：

1. 用户升级后，立即按新等级计算抽奖资格。
2. 用户降级或积分回滚时，需要按当前等级重新计算资格。
3. 抽奖次数以抽奖记录为准，不能只依赖前端缓存。
4. 管理员重置用户抽奖状态后，用户可重新获得一次对应周期抽奖资格。
5. 抽奖频率判断接口需要返回：
   - 当前 VIP 等级。
   - 是否可抽奖。
   - 剩余等待时间或下次可抽奖时间。
   - 本周期已抽奖次数。

验收标准：

1. VIP1 新用户可以抽 1 次。
2. VIP1 抽过后再次抽奖会被拦截。
3. VIP2 一周内第二次抽奖会被拦截。
4. VIP3 同一天第二次抽奖会被拦截。
5. VIP3 到第二天可再次抽奖。
6. 管理员重置后，用户可重新抽奖。

### 3.4 注册页面国家码下拉框

功能描述：
当前注册手机号需要用户手动输入国家码。五期要求注册页面提供国家码下拉选择，用户选择国家码后只填写本地手机号部分。

功能规则：

1. 注册页面手机号区域拆成两个控件：
   - 国家码下拉框，例如 `+234 Nigeria`、`+91 India`、`+233 Ghana`。
   - 本地手机号输入框。
2. 前端提交注册时，将国家码和本地手机号组合成完整手机号，例如 `+2348090001234`。
3. 默认国家码建议使用当前主要市场 `+234`。
4. 国家码列表至少包含：
   - `+234 Nigeria`
   - `+91 India`
   - `+233 Ghana`
   - `+86 China`
   - `+44 United Kingdom`
   - `+1 United States`
5. 国家码下拉项后续应可扩展，建议由前端常量开始，后续可改为后端配置接口。
6. 用户不能手动在手机号输入框里再输入 `+xx`；如果输入，需要前端清洗或提示。
7. 登录逻辑保持兼容完整手机号，不影响旧用户登录。

验收标准：

1. 注册页面可以选择国家码。
2. 用户选择 `+234` 并输入本地号码后，后端保存完整手机号。
3. 用户选择 `+91` 注册后，四期的国家码奖励逻辑仍能正确识别。
4. 用户不需要手动输入 `+`。
5. 旧账号手机号不受影响。

### 3.5 用户订单页中奖记录滚动窗

功能描述：
用户端订单页面顶部新增一个中奖记录滚动板块，沿用抽奖项目的中奖滚动窗效果，用来展示其他用户中奖记录，提升活动氛围。

页面位置：

涉及页面：`gift-chat-uni/src/pages/transactions/index.vue`

功能规则：

1. 在订单列表上方新增“中奖记录”或“Lucky winners”板块。
2. 滚动内容展示最近或随机中奖记录：
   - 脱敏手机号或用户名。
   - 奖品名称。
   - 中奖时间。
3. 滚动效果参考抽奖项目 `index.html` 的中奖名单滚动窗。
4. 中奖记录接口由 CardBrother 后端提供，不直接读取抽奖项目的 `users.json`。
5. 敏感信息必须脱敏展示，例如：
   - `+234 *** *** 1234`
   - `jo***th`
6. 如果暂无真实中奖记录，可显示预置演示记录；上线后优先显示真实记录。
7. 点击滚动窗可跳转抽奖页或打开抽奖入口。

验收标准：

1. 用户订单页顶部能看到中奖滚动板块。
2. 中奖记录自动滚动，不遮挡订单筛选和订单列表。
3. 显示内容脱敏。
4. 有真实中奖记录时优先展示真实记录。
5. 暂无真实记录时页面不报错，显示空状态或演示记录。

### 3.6 客服端搜索与国家分组

功能描述：
客服工作台需要增强客户查找和聊天记录查找能力。客服在顶部搜索栏中可以选择或自动识别搜索范围：搜索用户、搜索聊天记录。同时客户列表需要按手机号国家码自动分组，像 QQ 联系人分组一样，一个国家有第一个客户时自动出现该国家分组，分组下面展示对应客户。

功能规则：

1. 客服端搜索栏支持两个搜索类型：
   - 搜索用户。
   - 搜索聊天记录。
2. 搜索用户时，支持按以下字段匹配：
   - 客户备注。
   - 用户名。
   - 显示名。
   - 手机号。
   - 手机号国家码。
   - 邮箱。
   - VIP 等级。
3. 搜索聊天记录时，支持按以下字段匹配：
   - 当前客服有权限访问的客服会话消息内容。
   - 客户发送的文本消息。
   - 客服发送的文本消息。
   - 系统消息中的订单号、提现单号、抽奖记录关键词。
4. 搜索聊天记录结果需要展示：
   - 匹配客户。
   - 匹配消息摘要。
   - 消息发送时间。
   - 发送人角色。
5. 点击聊天记录搜索结果后，进入对应客户会话，并定位或高亮匹配消息；如果暂时无法精确定位消息，至少进入对应会话并展示匹配消息上下文。
6. 搜索权限必须以后端为准：
   - 客服只能搜索自己负责客户和会话。
   - 管理员可搜索全平台客户和会话。
7. 客服客户列表按国家码自动分组：
   - 例如 `+234 Nigeria`、`+91 India`、`+233 Ghana`。
   - 如果某国家码下有第一个客户，系统自动创建该国家分组。
   - 该国家码下没有客户时，不显示空分组。
8. 国家分组内的客户按以下优先级排序：
   - 未读消息在前。
   - 最近消息时间更新的在前。
   - 同时间按客户备注或用户名排序。
9. 分组本身按建议顺序展示：
   - 常用国家码优先，例如 `+234`、`+91`、`+233`。
   - 其他国家码按国家码或国家名称排序。
   - 无法识别国家码的客户进入“未知国家码”分组。
10. 分组支持折叠/展开，并记住客服上次展开状态。
11. 搜索时可以跨分组命中；搜索结果展示时仍标明客户所属国家分组。
12. 搜索栏为空时，恢复国家分组客户列表。

验收标准：

1. 客服在搜索栏选择“用户”并输入用户名，可以找到对应客户。
2. 客服输入客户备注，可以找到备注匹配客户。
3. 客服选择“聊天记录”并输入消息关键词，可以找到包含该关键词的会话消息。
4. 点击聊天记录搜索结果后，可以进入对应客户会话。
5. 客服不能搜索到其他客服负责客户的聊天记录。
6. 有 `+234` 客户时，客服列表自动出现 `+234 Nigeria` 分组。
7. 有 `+91` 客户时，客服列表自动出现 `+91 India` 分组。
8. 某国家没有客户时，不显示该国家空分组。
9. 未读客户在所在国家分组内排在前面。
10. 折叠分组后刷新页面，展开状态可保持。

### 3.7 用户侧聊天记录和订单记录隐藏

功能描述：
用户端需要提供“删除”聊天记录和订单记录的能力。这里的删除不是物理删除服务器数据，而是只对当前用户隐藏。用户手动删除后，自己端看不到对应聊天或订单；但服务器仍然保留原始记录，客服端和管理员端仍然可以查看完整聊天和订单，用于客服处理、风控、财务和审计。

功能规则：

1. 用户可在自己的聊天页面删除聊天记录：
   - 可删除单条消息在自己端的显示。
   - 可删除整个会话在自己端的显示，具体交互可做成“清空聊天记录”或“隐藏会话”。
2. 用户可在自己的订单页面删除/隐藏订单记录。
3. 删除/隐藏只影响当前用户自己的视图：
   - 不删除数据库中的原始消息。
   - 不删除数据库中的原始订单。
   - 不影响客服端查看。
   - 不影响管理员端查看。
4. 如果聊天双方都有同一条消息，用户 A 删除后只影响用户 A；客服或对方用户仍可看到自己有权限范围内的记录。
5. 用户隐藏订单后：
   - 用户订单列表不再显示该订单。
   - 用户订单搜索也不再搜到该订单。
   - 该订单仍参与后台统计、客服工作台、管理员订单管理和必要的余额/积分口径。
6. 对于未完成订单，是否允许用户隐藏需要谨慎：
   - 建议五期先允许隐藏所有状态，但隐藏前提示“订单仍由客服继续处理”。
   - 如果业务希望避免用户误解，可限制只有已完成、已取消、已争议结束的订单可隐藏。
7. 用户隐藏聊天或订单时需要二次确认，提示文案必须说明“仅从你的列表隐藏，客服和平台仍保留记录”。
8. 用户隐藏记录后，后端记录隐藏关系：
   - 用户 ID。
   - 目标类型：聊天消息、会话、订单。
   - 目标 ID。
   - 隐藏时间。
9. 用户可选是否需要“恢复隐藏记录”：
   - 五期暂定不做恢复入口。
   - 如果后续需要，可由客服或用户设置页提供“恢复已隐藏记录”。
10. 后端接口返回用户端列表时，需要自动排除该用户隐藏过的记录；客服端和管理员端接口不受影响。

验收标准：

1. 用户删除单条聊天消息后，刷新页面仍看不到该消息。
2. 用户清空/隐藏会话后，自己端会话列表不再显示该会话或不再显示历史消息。
3. 用户隐藏订单后，自己端订单列表不再显示该订单。
4. 客服端仍能看到用户隐藏的聊天记录和订单。
5. 管理员端仍能看到用户隐藏的聊天记录和订单。
6. 隐藏操作不会影响订单状态、余额、积分、VIP 等级和抽奖资格计算。
7. 隐藏操作有二次确认提示，明确说明不是服务器删除。

## 4. 页面改造范围

### 4.1 个人页

涉及页面：`gift-chat-uni/src/pages/me/index.vue`

改造点：

1. 增加 VIP 等级展示。
2. 增加等级进度条。
3. 展示当前积分、下一等级门槛、还差多少积分。
4. 增加抽奖入口，显示“可抽奖”或“下次可抽奖时间”。

### 4.2 抽奖页

建议新增页面：`gift-chat-uni/src/pages/lucky-wheel/index.vue`

改造点：

1. 迁移抽奖项目转盘 UI。
2. 展示当前 VIP 等级和抽奖资格。
3. 点击抽奖前调用资格接口。
4. 点击抽奖后调用后端抽奖接口。
5. 展示中奖结果和中奖记录滚动窗。

### 4.3 注册页

涉及页面：`gift-chat-uni/src/pages/register/index.vue`

改造点：

1. 手机号字段增加国家码下拉框。
2. 本地号码输入框不需要输入国家码。
3. 注册提交时拼接完整手机号。

### 4.4 用户订单页

涉及页面：`gift-chat-uni/src/pages/transactions/index.vue`

改造点：

1. 在订单页面顶部新增中奖滚动板块。
2. 复用抽奖项目中奖滚动窗风格。
3. 支持跳转抽奖页。
4. 订单卡片增加用户侧“删除/隐藏”操作。
5. 隐藏订单前弹出确认，说明该操作只影响用户自己端显示。

### 4.5 管理后台

涉及页面：`gift-chat-uni/src/pages/admin-console/index.vue`

改造点：

1. 增加 VIP 等级规则查看或配置区域。
2. 增加用户 VIP 等级、积分查询。
3. 增加中奖记录查询。
4. 增加奖品处理状态管理。
5. 增加管理员重置用户抽奖资格能力。

### 4.6 客服工作台

涉及页面：`gift-chat-uni/src/pages/support-chat-v2/index.vue`

改造点：

1. 顶部搜索栏增加搜索类型切换：用户、聊天记录。
2. 用户搜索结果直接在客户列表区域展示。
3. 聊天记录搜索结果展示消息摘要，并支持点击进入会话。
4. 客户列表改为按手机号国家码自动分组。
5. 国家分组支持折叠/展开，并显示该分组客户数和未读数。
6. 分组内未读客户优先展示，随后按最近消息时间排序。
7. 客户资料中继续展示国家码，和左侧分组保持一致。

### 4.7 用户聊天页

涉及页面：`gift-chat-uni/src/pages/support/index.vue`、`gift-chat-uni/src/pages/friends/index.vue`

改造点：

1. 单条消息长按菜单增加“删除”。
2. 会话更多菜单增加“清空聊天记录”或“隐藏会话”。
3. 删除前弹出确认，说明仅从当前用户设备/账号视图隐藏。
4. 删除后刷新页面仍保持隐藏状态。

## 5. 数据与接口影响

建议新增或扩展的数据：

1. VIP 等级配置表：
   - 等级名称。
   - 积分门槛。
   - 积分规则。
   - 抽奖频率。
   - 是否启用。
2. 用户积分记录表：
   - 用户。
   - 来源订单。
   - 变动积分。
   - 变动原因。
   - 创建时间。
3. 用户 VIP 汇总字段或视图：
   - 当前积分。
   - 当前 VIP 等级。
   - 下一等级。
   - 下一等级进度。
4. 抽奖奖品表：
   - 奖品名称。
   - 奖品类型。
   - 权重。
   - 图片。
   - 是否启用。
5. 抽奖记录表：
   - 用户。
   - 抽奖时 VIP 等级。
   - 奖品。
   - 抽奖时间。
   - 周期类型。
   - 发放状态。
   - 处理人。
6. 抽奖资格重置记录表：
   - 用户。
   - 操作管理员。
   - 重置原因。
   - 重置时间。
7. 客服搜索索引或查询视图：
   - 会话 ID。
   - 客户 ID。
   - 客户备注。
   - 用户名。
   - 手机号。
   - 国家码。
   - 最近消息摘要。
   - 最近消息时间。
8. 聊天记录搜索结果模型：
   - 消息 ID。
   - 会话 ID。
   - 客户 ID。
   - 发送人角色。
   - 消息摘要。
   - 命中关键词。
   - 发送时间。
9. 用户隐藏记录表：
   - 用户 ID。
   - 目标类型：`MESSAGE`、`CONVERSATION`、`ORDER`。
   - 目标 ID。
   - 隐藏范围：单条消息、整个会话、订单。
   - 隐藏时间。
   - 可选恢复时间。

建议新增接口：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/vip/me` | 获取当前用户 VIP 等级、积分和进度 |
| GET | `/api/lottery/eligibility` | 获取当前用户抽奖资格 |
| POST | `/api/lottery/spin` | 执行抽奖 |
| GET | `/api/lottery/winners` | 获取中奖滚动窗记录 |
| GET | `/api/admin/vip/users` | 管理员查看用户积分和等级 |
| GET | `/api/admin/lottery/records` | 管理员查看中奖记录 |
| POST | `/api/admin/lottery/records/{id}/status` | 管理员更新奖品发放状态 |
| POST | `/api/admin/lottery/users/{userId}/reset` | 管理员重置用户抽奖资格 |
| GET | `/api/support/search/customers` | 客服搜索自己负责的客户 |
| GET | `/api/support/search/messages` | 客服搜索自己负责客户的聊天记录 |
| GET | `/api/admin/search/customers` | 管理员搜索全平台客户 |
| GET | `/api/admin/search/messages` | 管理员搜索全平台聊天记录 |
| POST | `/api/account/hidden-records` | 用户隐藏聊天消息、会话或订单 |
| GET | `/api/account/hidden-records` | 用户查看自己的隐藏记录，后续可用于恢复 |
| DELETE | `/api/account/hidden-records/{id}` | 用户恢复某条隐藏记录，可选功能 |

## 6. 权限与安全要求

1. 积分和 VIP 等级必须由后端计算，前端不能传入最终等级。
2. 抽奖资格必须由后端判断，前端不能绕过。
3. 抽奖奖品必须由后端随机生成，前端不能传入最终奖品。
4. 用户只能查看自己的积分、等级和抽奖记录。
5. 管理员可以查看全平台积分和中奖记录。
6. 中奖滚动窗中的手机号、邮箱、用户名必须脱敏。
7. 抽奖接口需要防重复点击和并发重复抽奖。
8. 客服搜索必须按当前客服负责范围过滤，不能返回其他客服客户或聊天记录。
9. 聊天记录搜索结果只返回文本摘要，不直接暴露无权限附件或敏感字段。
10. 用户删除聊天或订单必须实现为用户维度隐藏，不允许物理删除原始业务记录。
11. 客服端和管理员端查询必须绕过用户隐藏过滤，仍展示完整记录。
12. 关键操作需要记录日志：订单计积分、积分回滚、抽奖、奖品发放、管理员重置抽奖资格、用户隐藏聊天或订单。

## 7. 五期验收总清单

| 编号 | 功能 | 验收结果 |
| --- | --- | --- |
| 1 | VIP 等级计算 | 用户按完成交易累计积分并自动升级 |
| 2 | 积分规则 | 交易 1 美刀获得 1 积分 |
| 3 | 等级门槛 | 20 积分 VIP2，100 积分 VIP3，500 积分 VIP4 |
| 4 | 个人页进度条 | 个人页下方显示 VIP 等级和升级进度 |
| 5 | 抽奖项目集成 | 当前用户可在 CardBrother 内抽奖，不需要进入独立抽奖账号体系 |
| 6 | VIP1 抽奖 | VIP1 新用户可抽 1 次 |
| 7 | VIP2 抽奖 | VIP2 每周可抽 1 次 |
| 8 | VIP3 抽奖 | VIP3 每天可抽 1 次 |
| 9 | 注册国家码下拉 | 注册页国家码可下拉选择，不需要手动输入 `+xx` |
| 10 | 订单页中奖滚动窗 | 用户订单页顶部展示中奖记录滚动板块 |
| 11 | 中奖记录脱敏 | 滚动窗手机号/用户名脱敏展示 |
| 12 | 管理员中奖管理 | 管理员可查看中奖记录并处理发放状态 |
| 13 | 客服搜索用户 | 客服可在搜索栏按备注、用户名、手机号、国家码搜索客户 |
| 14 | 客服搜索聊天记录 | 客服可在搜索栏按关键词搜索自己客户的聊天消息 |
| 15 | 搜索权限控制 | 客服不能搜索到其他客服负责客户或聊天记录 |
| 16 | 国家码自动分组 | 客户列表按国家码自动生成分组，有客户才显示分组 |
| 17 | 分组排序 | 分组内未读客户优先，随后按最近消息时间排序 |
| 18 | 用户删除聊天记录 | 用户删除聊天后自己端不可见，客服和管理员仍可见 |
| 19 | 用户删除订单记录 | 用户隐藏订单后自己端不可见，客服和管理员仍可见 |
| 20 | 删除不影响业务统计 | 隐藏订单不影响订单状态、余额、积分、VIP 等级和抽奖资格 |

## 8. 优先级建议

| 优先级 | 功能 |
| --- | --- |
| P0 | VIP 积分等级计算、个人页等级进度条、注册国家码下拉 |
| P0 | 客服搜索用户、搜索聊天记录、按国家码自动分组 |
| P0 | 用户侧聊天记录和订单记录隐藏，不影响客服/管理员查看 |
| P0 | 抽奖资格判断、抽奖接口、VIP1/VIP2/VIP3 频率限制 |
| P1 | 抽奖转盘 UI 迁移、用户订单页中奖滚动窗 |
| P1 | 中奖记录表、管理员中奖记录查询和发放状态 |
| P2 | VIP 等级规则后台配置、VIP4 专属权益、奖品权重后台配置 |

## 9. 待确认问题

1. 积分按订单的哪个字段计算：礼品卡面值 USD、结算金额折算 USD，还是用户实际到账金额？
2. 如果订单不是 USD 面值，是否需要按照汇率换算成 USD 积分？
3. VIP1 新用户抽奖是否只限注册后一次，还是每天/每周也有基础次数？
4. VIP4 是否要比 VIP3 有更高权益，例如每天 2 次、更高中奖概率或专属奖池？
5. 现金类奖品是否自动进入余额，还是只生成中奖记录由管理员人工发放？
6. 奖品池和中奖概率是否需要后台可配置？
7. 抽奖项目里原有邮箱验证码、独立账号、JSON 文件是否完全废弃，只保留转盘 UI 和中奖滚动效果？
8. 中奖滚动窗是否显示真实中奖记录，还是允许混合演示记录？
9. 注册国家码下拉列表是否需要支持搜索国家名称？
10. 后续是否需要把抽奖入口放到首页、个人页、订单页多个位置？
11. 客服聊天记录搜索范围是否包含图片、GIF、语音、视频通话卡片等非文本消息？
12. 聊天记录搜索结果是否需要精确滚动到消息位置，还是进入会话并显示上下文即可？
13. 国家分组名称是只显示国家码，还是显示“国家码 + 国家名 + 客户数”？
14. 客服是否允许手动调整客户分组，还是五期只按国家码自动分组？
15. 用户侧删除聊天记录是否需要支持单条消息、整个会话，还是两个都做？
16. 用户侧隐藏订单是否允许隐藏未完成订单，还是只允许隐藏已完成/已取消订单？
17. 用户隐藏记录后是否需要提供恢复入口？
18. 好友聊天记录是否也支持用户侧隐藏，还是五期只做客服聊天和订单？

## 10. 建议开发拆分

第一阶段：VIP 等级和注册国家码

1. 增加 VIP 等级计算服务。
2. 订单完成后生成积分记录。
3. 个人页增加 VIP 等级进度条。
4. 注册页增加国家码下拉框。
5. 客服工作台增加国家码自动分组。
6. 客服工作台增加用户搜索和聊天记录搜索。
7. 用户端增加聊天记录和订单记录隐藏能力。

第二阶段：抽奖后端能力

1. 设计抽奖奖品表和抽奖记录表。
2. 实现抽奖资格接口。
3. 实现抽奖接口和频率限制。
4. 实现中奖记录滚动接口。
5. 管理员增加中奖记录查询和奖品发放状态。

第三阶段：抽奖前端集成

1. 从 `W:\cyproject\7-choujiang\choujiang` 迁移转盘视觉和奖品素材。
2. 新增 CardBrother 抽奖页。
3. 接入抽奖资格、抽奖结果和中奖记录。
4. 用户订单页顶部增加中奖滚动窗。
