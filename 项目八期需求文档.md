# Xcard 项目八期需求文档

版本：v0.8 草案
日期：2026-07-12
来源：注册、登录国家绑定与多币种金额展示反馈
用途：用于确认八期国家绑定、登录国家校验、美元统一参考单位、双汇率体系、钱包与转盘本地币种结算、历史账号迁移和验收标准。

## 1. 八期目标

八期重点围绕“注册国家永久绑定、普通用户登录国家强校验、美元统一参考单位、不同国家使用不同本地币种、礼品卡业务汇率与普通货币汇率分离”展开。

当前系统虽然在登录页和注册页显示国家选择，但国家主要保存在前端临时状态中，后端账号没有独立、稳定的国家字段。登录请求也没有国家参数，因此用户在登录页选择的国家不会参与账号校验。

当前金额体系还存在以下问题：

1. 礼品卡汇率使用自由文本保存，首页可能只显示 `1000 RATE`，用户无法直接判断 `1000` 的币种、基准单位和业务含义。
2. 卖卡页面通过从汇率文本中提取数字进行计算，格式变化后容易计算错误。
3. 钱包、交易、转盘、提现、奖励等金额大量使用字符串保存，缺少统一的币种代码和汇率快照。
4. 部分页面默认使用奈拉或写死 `NGN`，不能正确服务印度、喀麦隆、加纳、肯尼亚和美国用户。
5. 用户可以在前端切换国家查看不同地区内容，但切换结果没有与账号身份绑定。
6. 礼品卡业务汇率和普通货币换算汇率的用途不同，不能使用同一张表或同一个数值代替。

八期核心目标：

1. 普通用户注册时必须选择国家，注册成功后账号永久绑定该国家。
2. 增加美国注册与登录选项。
3. 普通用户登录时必须提交所选国家；账号、密码和国家三项共同校验。
4. 所选国家与账号绑定国家不一致时，即使账号密码正确也不能登录。
5. 客服和管理员登录不受国家校验限制。
6. 美元 `USD` 作为全系统统一参考单位，不以奈拉作为基准币种。
7. 普通用户实际看到和使用的币种由注册国家决定，不允许自行切换。
8. 礼品卡业务汇率与普通货币汇率分开维护。
9. 礼品卡汇率明确表示“每 1 USD 礼品卡面值可结算多少当地币”。
10. 转盘现金奖品、注册奖励、推荐奖励等统一先定义美元金额，再按获奖或入账时的普通货币汇率换算为用户当地币。
11. 钱包以用户绑定国家的当地币种记账；已入账金额不因后续汇率变化而改变。
12. 历史普通用户尽量按手机号区号自动绑定；无法识别的账号禁止登录，等待管理员补绑国家。

## 2. 核心业务原则

### 2.1 国家是普通用户账号属性

1. 国家不再只是前端筛选条件，而是普通用户账号的持久化身份属性。
2. 注册时必须明确保存国家代码，不能只依赖手机号临时推断。
3. 手机号国家区号必须与注册选择国家一致。
4. 普通用户注册成功后不能自行修改国家。
5. 普通用户不能通过清理缓存、换设备、修改前端状态或直接调用接口绕过国家绑定。
6. 客服和管理员可以不绑定业务国家，登录时不执行国家一致性校验。
7. 管理员可以为历史未识别账号补绑国家，也可以在受控流程中纠正错误绑定。

### 2.2 美元是统一参考单位

1. 系统统一使用 `USD` 作为跨国家金额定义和换算参考单位。
2. “美元作为统一参考单位”不等于所有用户的钱包都显示美元。
3. 用户钱包币种、结算币种和现金奖励显示币种由账号绑定国家决定。
4. 美国用户直接使用 USD，不需要二次换算。
5. 非美国用户在金额入账时，将美元参考金额按当时有效汇率换算为当地币。
6. 换算完成后必须保存当地币金额、币种代码和汇率快照。
7. 后续管理员修改普通货币汇率时，不重新计算已经入账的钱包余额、历史订单、历史奖励或历史提现。

### 2.3 两套汇率必须分离

八期必须建立两套用途不同的汇率体系。

| 汇率类型 | 用途 | 示例 | 是否按卡片区分 |
| --- | --- | --- | --- |
| 礼品卡业务汇率 | 计算用户出售礼品卡时可获得的当地币结算金额 | Nigeria Steam：`$1 = ₦1,000` | 是 |
| 普通货币汇率 | 转盘现金奖品、注册奖励、推荐奖励及其他 USD 金额换算为用户当地币 | Nigeria：`$1 = ₦1,500` | 否 |

规则：

1. 两套汇率不能共用同一字段表达业务含义。
2. 礼品卡通常按低价回收，因此礼品卡业务汇率可以低于普通货币汇率。
3. 同一个国家的 Steam、Apple / iTunes、Razer Gold 等卡片可以配置不同业务汇率。
4. 普通货币汇率只按币种或国家维护，不按礼品卡品牌区分。
5. 两套汇率均由管理员手动维护，八期不接第三方实时汇率接口。
6. 管理员修改汇率只影响修改后新产生的计算，不改写历史金额快照。

## 3. 首批支持国家与币种

八期首批支持以下 6 个国家：

| 国家代码 | 国家名称 | 电话区号 | 本地号码长度 | 钱包币种 | 币种符号 |
| --- | --- | --- | --- | --- | --- |
| `NG` | Nigeria | `+234` | 10 位 | `NGN` | `₦` |
| `IN` | India | `+91` | 10 位 | `INR` | `₹` |
| `CM` | Cameroon | `+237` | 9 位 | `XAF` | `FCFA` |
| `GH` | Ghana | `+233` | 9 位 | `GHS` | `GH₵` |
| `KE` | Kenya | `+254` | 9 位 | `KES` | `KSh` |
| `US` | United States | `+1` | 10 位 | `USD` | `$` |

国家目录规则：

1. 登录页、注册页、管理员汇率页和后端国家校验必须复用同一份国家目录。
2. 前端不能维护一套国家列表、后端再维护另一套不同列表。
3. 国家代码使用 ISO 两位代码。
4. 币种代码使用 ISO 4217 代码。
5. 美国在界面中统一显示 `United States`，不混用 `America`、`USA`、`US` 作为展示名称。
6. `+1` 区号在八期国家目录中仅代表用户主动选择的 United States；注册请求仍必须显式提交 `US`。
7. 后续增加国家时，必须同时配置国家代码、电话区号、号码长度、币种代码、币种符号和普通货币汇率。

## 4. 角色范围

| 角色 | 八期国家与币种规则 |
| --- | --- |
| 普通用户 | 注册时绑定国家；登录时强校验国家；只能使用绑定国家币种；不能自行修改国家 |
| 客服 | 登录不受国家限制；可以查看客户绑定国家、币种和金额快照；不能代替普通用户在前端随意切换钱包币种 |
| 管理员 | 登录不受国家限制；维护两套汇率；处理历史账号国家补绑；查看国家与汇率变更记录 |

## 5. 注册国家绑定

### 5.1 注册页面

注册页面必须提供国家选择，默认选项可保持 Nigeria，但用户提交前必须明确看到当前选择。

国家选项显示：

1. 国家名称。
2. 电话区号。
3. 可选国旗或国家缩写，但不能只用国旗表达国家。
4. 美国选项显示为 `+1 United States`。

注册表单至少包含：

1. 国家。
2. 用户名。
3. 邮箱。
4. 手机号。
5. 密码。
6. 邀请码。

### 5.2 注册校验

1. 普通用户注册请求必须提交 `countryCode`。
2. `countryCode` 必须在启用的国家目录中。
3. 手机号必须使用所选国家的电话区号和号码长度规则。
4. 前端提交本地手机号时，后端负责组合并标准化完整国际号码。
5. 前端即使提交完整国际号码，后端也必须检查号码区号与 `countryCode` 一致。
6. 选择 United States 时只接受符合 `+1` 和 10 位本地号码规则的手机号。
7. 注册成功后，在账号中保存国家代码、国家名称和钱包币种代码。
8. 钱包币种由国家目录决定，客户端不能自行提交任意币种。
9. 注册返回的会话信息必须包含国家代码、国家名称、币种代码和币种符号。
10. 注册奖励按用户国家的普通货币汇率或该国家明确配置的奖励规则入账，并保存金额快照。

### 5.3 国家不可自行修改

1. 普通用户个人资料页可以查看绑定国家和钱包币种。
2. 普通用户个人资料页不提供国家修改入口。
3. 更新个人资料接口不能接受普通用户修改国家或币种。
4. 用户重新安装应用、清理缓存或换设备后，国家仍从后端账号资料恢复。
5. 管理员纠正国家时必须记录修改前国家、修改后国家、操作管理员、原因和时间。
6. 已有钱包余额、已完成订单或已完成奖励的账号纠正国家时，不允许静默重算历史金额。
7. 涉及已有财务数据的国家纠正应进入人工处理流程，保留原金额与币种快照。

## 6. 登录国家强校验

### 6.1 登录页面

登录页面沿用需求图片中的国家下拉交互，并加入 United States。

下拉顺序：

1. Nigeria
2. India
3. Cameroon
4. Ghana
5. Kenya
6. United States

登录表单包含：

1. Country。
2. Email、username 或 phone。
3. Password。
4. Sign in 按钮。

页面规则：

1. 国家下拉必须完整显示国家名称。
2. 移动端选择面板不能被键盘或底部安全区遮挡。
3. 登录成功后，前端选择国家必须以服务端返回的绑定国家为准。
4. 普通用户登录后不能继续使用登录前缓存的其他国家。

### 6.2 普通用户登录校验

1. 登录请求增加必填 `countryCode`。
2. 后端先按 username、email 或 phone 查找账号，再验证状态、密码和国家。
3. 普通用户账号密码正确但国家不一致时，登录失败且不签发令牌。
4. 使用 username 登录时也必须校验国家。
5. 使用 email 登录时也必须校验国家。
6. 使用 phone 登录时，前端允许输入本地号码，并根据所选国家组合国际区号。
7. 手机号登录最终仍以账号保存的国家代码为准，不能只比较手机号前缀。
8. 登录失败提示统一使用 `Invalid account, password, or country`，避免单独泄露账号是否存在或实际绑定国家。
9. 国家不匹配计入登录失败次数和登录限流。
10. 登录限流至少继续按账号标识和客户端 IP 生效。

### 6.3 客服和管理员登录

1. 客服和管理员使用现有账号密码登录流程。
2. 客服和管理员不受国家一致性校验限制。
3. 因登录前无法确定账号角色，前端仍可显示国家选择；后端识别角色后跳过国家校验。
4. 客服和管理员会话可以不返回业务国家，也可以返回空国家字段。
5. 客服和管理员进入后台后可跨国家查看授权范围内的数据。

## 7. 历史账号迁移

### 7.1 普通用户自动绑定

1. 新迁移为普通用户增加国家代码、币种代码和国家绑定状态。
2. 对已有普通用户，根据标准化手机号最长区号优先匹配国家。
3. `+234` 绑定 Nigeria。
4. `+91` 绑定 India。
5. `+237` 绑定 Cameroon。
6. `+233` 绑定 Ghana。
7. `+254` 绑定 Kenya。
8. `+1` 绑定 United States。
9. 成功识别后，将绑定状态标记为 `BOUND`。

### 7.2 无法识别账号

1. 手机号为空、格式错误、区号不在启用目录或无法唯一识别时，将绑定状态标记为 `UNRESOLVED`。
2. `UNRESOLVED` 普通用户禁止登录。
3. 系统不将无法识别的账号默认绑定 Nigeria。
4. 管理员后台提供待补绑账号列表。
5. 管理员选择正确国家并填写原因后完成补绑。
6. 补绑完成后，账号绑定状态变为 `BOUND`，用户才可以按该国家登录。
7. 客服和管理员历史账号不要求补绑国家，不受此迁移阻塞。

### 7.3 历史金额兼容

1. 历史订单、钱包、提现、奖励和转盘记录保留原始金额文本，不批量猜测或重算。
2. 能明确识别币种的历史记录可以回填币种代码，但必须保留原显示快照。
3. 无法识别币种的历史记录标记为历史兼容数据，由客服和管理员按原文本查看。
4. 新金额模型只对八期上线后新产生的数据强制保存结构化金额和汇率快照。
5. 不允许因为用户补绑国家而自动修改历史金额。

## 8. 礼品卡业务汇率

### 8.1 `1000 RATE` 的准确含义

图片中的 Steam `1000 RATE` 应解释为：

```text
$1 Steam 礼品卡面值 = ₦1,000 用户结算金额
```

示例：

```text
礼品卡：Steam
礼品卡面值：$100
用户国家：Nigeria
Steam Nigeria 业务汇率：$1 = ₦1,000
预计结算：$100 × ₦1,000/USD = ₦100,000
```

### 8.2 展示规则

首页不再显示：

```text
1000
RATE
```

应显示：

```text
$1 = ₦1,000
Payout rate
```

不同国家示例：

| 用户国家 | 示例展示 |
| --- | --- |
| Nigeria | `$1 = ₦1,000` |
| India | `$1 = ₹70` |
| Cameroon | `$1 = FCFA 500` |
| Ghana | `$1 = GH₵ 10` |
| Kenya | `$1 = KSh 120` |
| United States | `$1 = $0.85` |

表中数值仅为格式示例，真实数值由管理员维护。

### 8.3 数据规则

礼品卡业务汇率至少包含：

1. 礼品卡代码。
2. 礼品卡标准名称。
3. 结算国家代码。
4. 结算币种代码。
5. 每 1 USD 礼品卡面值的当地币结算金额 `localPayoutPerUsd`。
6. 状态。
7. 更新时间。
8. 更新管理员。

规则：

1. 业务汇率必须使用数值字段保存，不能继续只保存任意自由文本。
2. 币种由结算国家自动确定，管理员不能为 Nigeria 汇率提交 INR。
3. 同一礼品卡、同一结算国家只能有一条当前有效业务汇率。
4. 不同礼品卡可以有不同业务汇率。
5. 普通用户只读取账号绑定国家的有效业务汇率。
6. 前端不再依赖临时 `selectedCountryCode` 决定可见汇率。
7. 卖卡订单必须保存业务汇率数值、币种和显示文本快照。
8. 管理员以后调整业务汇率，不改写已提交订单的预计结算金额。

### 8.4 非 USD 礼品卡面值

美元是统一参考单位。若礼品卡本身使用 AUD、EUR、GBP 等非 USD 面值：

1. 先使用管理员维护的普通货币汇率将卡片面值换算为 USD 参考金额。
2. 再使用该礼品卡在用户国家的业务汇率计算当地币结算金额。
3. 订单必须保存卡片原始面值、卡片币种、换算后的 USD 金额、卡片币种兑 USD 汇率快照和礼品卡业务汇率快照。
4. 如缺少卡片币种兑 USD 汇率，不允许提交预计金额不确定的订单。

计算公式：

```text
USD 参考金额 = 卡片面值 × 卡片币种兑 USD 汇率
当地币结算金额 = USD 参考金额 × 礼品卡当地业务汇率
```

## 9. 普通货币汇率

### 9.1 管理员手动维护

八期不接第三方实时汇率接口，由管理员在后台手动维护普通货币汇率。

普通货币汇率至少包含：

1. 国家代码。
2. 国家名称。
3. 币种代码。
4. 每 1 USD 可兑换的当地币金额 `localCurrencyPerUsd`。
5. 是否启用。
6. 更新时间。
7. 更新管理员。
8. 备注。

示例：

| 国家 | 币种 | 汇率格式 |
| --- | --- | --- |
| Nigeria | NGN | `$1 = ₦1,500` |
| India | INR | `$1 = ₹83` |
| Cameroon | XAF | `$1 = FCFA 600` |
| Ghana | GHS | `$1 = GH₵ 15` |
| Kenya | KES | `$1 = KSh 130` |
| United States | USD | `$1 = $1` |

表中非美国数值仅为格式示例，真实数值由管理员维护。

### 9.2 生效规则

1. 每个启用国家必须有一条有效普通货币汇率。
2. United States 固定为 `1 USD = 1 USD`，管理员不能保存为 0 或负数。
3. 汇率必须大于 0，并限制合理的小数位数。
4. 管理员修改汇率时记录旧值、新值、操作人和时间。
5. 新汇率只影响修改后发生的新入账、新奖励和新换算。
6. 历史数据使用各自保存的汇率快照。
7. 某国家普通汇率缺失或暂停时，该国家的新现金奖励和需要换算的业务必须停止，并提示管理员处理。
8. 普通货币汇率不能直接代替礼品卡业务汇率。

## 10. 转盘与现金奖励

### 10.1 奖品定义

1. 转盘现金奖品统一使用 USD 定义基础金额。
2. 物品奖品继续使用名称和图片，不参与货币换算。
3. 现金奖品不能再只把 `₦100`、`₦500` 等币种符号写进奖品名称。
4. 现金奖品至少保存奖品类型、USD 基础金额、显示名称、权重和状态。

### 10.2 用户展示

假设转盘现金奖品基础金额为 `$1`：

| 用户国家 | 用户看到的金额 |
| --- | --- |
| Nigeria | 按获奖时普通汇率显示 `₦...` |
| India | 按获奖时普通汇率显示 `₹...` |
| Cameroon | 按获奖时普通汇率显示 `FCFA ...` |
| Ghana | 按获奖时普通汇率显示 `GH₵...` |
| Kenya | 按获奖时普通汇率显示 `KSh ...` |
| United States | 显示 `$1` |

规则：

1. 用户打开转盘时，现金奖项按账号绑定国家预览当地币金额。
2. 用户获奖时，以获奖瞬间有效普通货币汇率计算实际当地币金额。
3. 获奖记录保存 USD 基础金额、当地币金额、币种代码和普通汇率快照。
4. 获奖后的记录、提现申请和客服资料使用快照金额，不随汇率变化。
5. 不允许用户通过切换前端国家改变奖品币种或获奖金额。
6. 管理员看到记录时同时显示 USD 基础金额和用户当地币金额。

## 11. 钱包、订单与其他金额

### 11.1 钱包

1. 每个普通用户钱包只能有一个主币种，即账号绑定国家币种。
2. Nigeria 用户钱包显示 NGN。
3. India 用户钱包显示 INR。
4. Cameroon 用户钱包显示 XAF。
5. Ghana 用户钱包显示 GHS。
6. Kenya 用户钱包显示 KES。
7. United States 用户钱包显示 USD。
8. 钱包余额不允许由用户手动切换显示币种。
9. 钱包可用、待结算和已提现金额必须返回币种代码和格式化金额。
10. 汇率变化不重新估值已有钱包余额。

### 11.2 卖卡订单

1. 用户只能使用绑定国家的礼品卡业务汇率计算结算金额。
2. 订单保存礼品卡原始面值和原始币种。
3. 订单保存 USD 参考金额。
4. 订单保存当地币结算金额和币种。
5. 订单保存参与计算的所有汇率快照。
6. 客服和管理员查看订单时不能用当前汇率重算历史结算金额。

### 11.3 提现、奖励和账务

以下业务均必须带币种并遵循金额快照规则：

1. 普通钱包提现。
2. 转盘现金奖品提现。
3. 注册奖励。
4. 推荐注册奖励。
5. 交易返利。
6. 客服账务汇总。
7. 管理员账务汇总。
8. 用户交易记录。
9. 客服客户资料。

客服或管理员跨国家汇总时：

1. 不允许直接把 NGN、INR、USD 等不同币种数值相加后显示为一个总数。
2. 默认按币种分别汇总。
3. 如需统一 USD 报表，必须显式使用报表汇率并标记为估算值；该能力不作为八期必须项。

## 12. 管理员功能

### 12.1 普通货币汇率管理

管理员可以：

1. 查看 6 个国家及其币种。
2. 设置每 1 USD 对应的当地币金额。
3. 启用或暂停某国家普通汇率。
4. 查看更新时间和更新人。
5. 查看历史变更记录。

### 12.2 礼品卡业务汇率管理

管理员可以：

1. 选择礼品卡。
2. 选择结算国家。
3. 查看自动确定的结算币种。
4. 输入每 1 USD 礼品卡面值的当地币结算金额。
5. 查看格式化预览，例如 `$1 = ₦1,000`。
6. 启用、暂停、编辑或删除业务汇率。
7. 不能再只输入含义不明确的自由文本 `1000`。

### 12.3 历史账号国家补绑

管理员可以：

1. 查看国家状态为 `UNRESOLVED` 的普通用户。
2. 查看用户名、邮箱、手机号和注册时间。
3. 选择正确国家。
4. 填写补绑原因。
5. 完成绑定并记录审计信息。
6. 对已有财务数据的账号显示风险提示，不自动重算历史数据。

## 13. 数据模型建议

### 13.1 普通用户国家字段

`app_user` 建议新增：

1. `country_code`：普通用户绑定国家代码，可为空以兼容客服和管理员。
2. `currency_code`：普通用户钱包币种，可为空以兼容客服和管理员。
3. `country_binding_status`：`BOUND` 或 `UNRESOLVED`。
4. `country_bound_at`：绑定时间。
5. `country_bound_by`：管理员补绑时记录管理员账号。

国家名称和币种符号优先从统一国家目录读取，避免重复存储后产生不一致。

### 13.2 普通货币汇率表

建议新增 `currency_exchange_rate`：

1. `id`。
2. `country_code`。
3. `currency_code`。
4. `local_currency_per_usd DECIMAL`。
5. `enabled`。
6. `note`。
7. `updated_by`。
8. `created_at`。
9. `updated_at`。

### 13.3 礼品卡业务汇率

现有 `gift_card_rate` 建议结构化：

1. 保留 `card_name` 和 `card_code`。
2. 将地区统一为标准 `country_code`。
3. 增加 `currency_code`。
4. 增加数值字段 `local_payout_per_usd DECIMAL`。
5. 现有 `rate_value` 只作为迁移兼容字段或显示快照，不再作为计算来源。
6. 对现有可解析的裸数字汇率按所属国家回填结构化数值。
7. 无法解析的旧汇率暂停使用并交由管理员确认。

### 13.4 金额快照

涉及金额的订单、奖励、转盘记录和钱包流水建议统一保存：

1. `base_amount_usd`。
2. `local_amount`。
3. `currency_code`。
4. `exchange_rate_snapshot`。
5. `business_rate_snapshot`，仅礼品卡业务需要。
6. `display_amount_snapshot`，用于兼容历史展示和审计。

数据库变更必须使用新的 Flyway 迁移文件，禁止修改已经应用的旧迁移。

## 14. 接口影响建议

### 14.1 注册接口

注册请求增加：

```json
{
  "countryCode": "US"
}
```

后端根据国家目录确定电话区号和币种，不能相信客户端提交的自定义币种。

### 14.2 登录接口

登录请求增加：

```json
{
  "identifier": "user@example.com",
  "password": "********",
  "countryCode": "US"
}
```

普通用户校验国家；客服和管理员跳过国家一致性校验。

### 14.3 会话接口

普通用户会话返回增加：

1. `countryCode`。
2. `countryName`。
3. `currencyCode`。
4. `currencySymbol`。

前端所有普通用户页面以服务端会话国家为准。

### 14.4 汇率接口

1. 礼品卡业务汇率返回 `countryCode`、`currencyCode` 和 `localPayoutPerUsd`。
2. 普通货币汇率使用独立管理员接口。
3. 普通用户汇率接口只返回绑定国家可见数据。
4. 后端负责格式化所需的结构化字段，前端负责按本地化规则展示。

### 14.5 钱包与记录接口

余额、交易、奖励、提现、转盘记录返回至少包含：

1. 数值金额。
2. 币种代码。
3. 格式化金额。
4. 必要时返回 USD 基础金额和汇率快照供后台审计。

## 15. 安全与异常处理

1. 国家校验必须在后端执行，不能依赖前端隐藏选项。
2. 普通用户令牌签发前必须完成国家校验。
3. 普通用户会话恢复时再次读取服务端国家，不使用本地国家作为授权依据。
4. 登录国家不匹配使用统一错误提示，不泄露账号真实国家。
5. 国家不匹配计入登录限流。
6. 国家、币种和汇率字段必须使用允许目录或严格数值校验。
7. 金额计算使用 `BigDecimal` 或等价高精度十进制类型，不能使用浮点数直接处理财务金额。
8. 金额舍入规则必须按币种统一定义，并在后端执行。
9. 汇率为 0、负数、空值或超出合理范围时拒绝保存。
10. 缺少有效普通货币汇率时，不允许静默使用 1 或默认 Nigeria 汇率。
11. 缺少礼品卡业务汇率时，不允许用户提交该卡片的卖卡订单。
12. 管理员国家补绑和汇率修改必须记录审计信息。

## 16. 非功能要求

### 16.1 一致性

1. 登录、注册、个人资料、首页、卖卡、转盘、钱包、交易、提现和客服资料使用同一国家与币种模型。
2. 同一金额在不同页面显示的币种和数值必须一致。
3. 同一订单在用户、客服和管理员页面使用同一金额快照。

### 16.2 精度

1. USD 基础金额和汇率使用十进制数值保存。
2. 不同币种允许配置显示小数位规则。
3. 计算过程保留足够精度，最终入账时统一舍入。
4. 页面格式化不能改变后端实际入账数值。

### 16.3 可审计性

1. 所有财务记录可以追溯使用的国家、币种和汇率快照。
2. 管理员能够解释历史金额为何与当前汇率计算结果不同。
3. 国家补绑和汇率变更必须可查询操作人和时间。

### 16.4 兼容性

1. 历史字符串金额继续可读。
2. 历史订单不因八期迁移丢失。
3. 客服和管理员现有登录不被国家字段阻断。
4. 现有 HTTPS、WebSocket、上传、聊天和视频流程不受影响。

## 17. 验收标准

### 17.1 注册与国家绑定

1. 注册页显示 Nigeria、India、Cameroon、Ghana、Kenya、United States。
2. 选择 United States 后显示 `+1`，只接受 10 位本地号码。
3. 注册请求保存 `US` 和 `USD`，而不是只保存 `+1` 手机号。
4. 选择 Nigeria 但提交 `+91` 手机号时，后端拒绝注册。
5. 注册成功后，刷新页面和换设备仍恢复账号绑定国家。
6. 普通用户资料页只能查看国家，不能修改国家。

### 17.2 登录

1. Nigeria 用户选择 Nigeria 且账号密码正确时登录成功。
2. Nigeria 用户选择 India 时，即使账号密码正确也登录失败。
3. 使用 username、email 和 phone 登录都执行国家校验。
4. 国家不匹配不签发访问令牌。
5. 客服和管理员无论登录页选择哪个国家，账号密码正确时均可登录。
6. 登录失败提示不泄露账号真实绑定国家。

### 17.3 礼品卡汇率

1. 首页不再显示含义不明确的 `1000 RATE`。
2. Nigeria Steam 汇率以 `$1 = ₦1,000` 格式显示。
3. `$100` Steam 卡按 `₦1,000/USD` 计算为 `₦100,000`。
4. India 用户只看到 India 礼品卡业务汇率。
5. United States 用户只看到 United States 礼品卡业务汇率并使用 USD 结算。
6. 修改 Steam 汇率不影响 Apple / iTunes 汇率。
7. 修改当前汇率不改变历史订单金额。

### 17.4 普通货币汇率与转盘

1. 管理员可以分别维护 6 个国家的普通货币汇率。
2. 礼品卡业务汇率和普通货币汇率显示在不同管理区域。
3. 转盘现金奖品以 USD 定义。
4. 不同国家用户看到同一 USD 奖品对应的不同当地币金额。
5. 获奖记录保存 USD 金额、当地币金额、币种和汇率快照。
6. 获奖后修改普通汇率，历史获奖金额保持不变。

### 17.5 钱包与账务

1. Nigeria 用户钱包只显示 NGN。
2. India 用户钱包只显示 INR。
3. Cameroon 用户钱包只显示 XAF。
4. Ghana 用户钱包只显示 GHS。
5. Kenya 用户钱包只显示 KES。
6. United States 用户钱包只显示 USD。
7. 用户不能通过切换前端国家改变钱包币种。
8. 汇率变化不改变已有钱包余额。
9. 客服查看客户资料时能看到客户国家和钱包币种。
10. 管理员跨国家汇总时，不直接相加不同币种。

### 17.6 历史账号

1. 可识别手机号的历史普通用户自动绑定正确国家。
2. 无法识别的历史普通用户状态为 `UNRESOLVED`。
3. `UNRESOLVED` 普通用户不能登录。
4. 管理员补绑后用户可以按新绑定国家登录。
5. 系统不会把无法识别的账号默认绑定 Nigeria。
6. 历史客服和管理员账号不因国家为空而无法登录。

## 18. 测试范围

### 18.1 后端测试

1. 6 个国家注册成功与号码长度校验。
2. 注册国家和手机号区号不一致校验。
3. 普通用户登录国家一致和不一致测试。
4. 客服、管理员国家豁免测试。
5. 历史用户自动绑定和未识别阻断测试。
6. 普通货币汇率增删改查、权限和审计测试。
7. 礼品卡业务汇率结构化校验测试。
8. 卖卡金额高精度计算和快照测试。
9. 转盘现金奖品换算和快照测试。
10. 钱包单币种和不同币种汇总隔离测试。
11. 汇率修改后历史金额不变测试。

### 18.2 前端测试

1. 登录国家下拉布局与图片要求一致。
2. 登录和注册均显示 United States。
3. 不同国家号码帮助文字正确。
4. 会话恢复后以前端服务端绑定国家为准。
5. 首页业务汇率格式显示正确。
6. 卖卡预计结算金额计算正确。
7. 转盘、钱包、交易、提现使用正确币种符号。
8. 普通用户界面没有国家修改入口。
9. 手机端国家选择面板、键盘和安全区不重叠。

### 18.3 回归测试

1. 用户、客服和管理员原有角色路由正常。
2. 客服聊天、未读角标、翻译和客户备注正常。
3. 卖卡图片上传和订单聊天流正常。
4. 提现、贷款、推荐奖励和注册奖励正常。
5. WebSocket、视频通话和推送流程不受影响。
6. Flyway 从现有生产版本升级成功。

## 19. 非目标与后续项

八期暂不包含：

1. 第三方实时外汇接口。
2. 用户自行修改国家。
3. 用户自行切换钱包币种。
4. 一个用户同时拥有多个币种钱包。
5. 自动把所有历史字符串金额重算为新结构。
6. 自动将不同币种账务汇总为 USD 财务报表。
7. 加密货币钱包或数字资产汇率。
8. 根据 IP、GPS 或 SIM 卡自动决定账号国家。

后续可评估：

1. 第三方外汇接口与手动汇率的容灾策略。
2. 多币种钱包。
3. 管理员统一 USD 估值报表。
4. 国家修改审批流程。
5. 汇率生效时间和定时发布。

## 20. 推荐实施顺序

1. 建立统一国家与币种目录，加入 United States。
2. 增加普通用户国家字段、绑定状态和历史账号迁移。
3. 调整注册请求与手机号国家一致性校验。
4. 调整登录请求、普通用户国家强校验和角色豁免。
5. 建立管理员手动维护的普通货币汇率。
6. 将礼品卡业务汇率从自由文本升级为结构化数值。
7. 为订单、奖励、转盘和钱包流水增加金额及汇率快照。
8. 改造首页、卖卡、转盘、钱包、提现和客服资料显示。
9. 补齐历史数据兼容、权限、精度和回归测试。
10. 在测试环境完成多国家账号全流程验收后再部署生产环境。

## 21. 八期确认结论

本需求文档按以下已确认决策编写：

1. 美元是全系统统一参考单位。
2. 普通用户注册国家决定账号国家和钱包币种。
3. 普通用户登录必须校验所选国家与绑定国家一致。
4. 美国加入登录和注册国家列表。
5. 普通用户禁止自行修改国家。
6. 客服和管理员登录不受国家限制。
7. 礼品卡业务汇率与普通货币汇率分开维护。
8. 普通货币汇率由管理员手动维护。
9. 钱包和历史记录使用入账时汇率快照，不因后续汇率变化而重算。
10. 历史普通用户按手机号自动绑定；无法识别时禁止登录并由管理员补绑。
