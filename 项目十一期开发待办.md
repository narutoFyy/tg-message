# Xcard 项目十一期开发待办

版本：v0.1 需求记录
日期：2026-08-12
范围：用户端客服消息全局提醒、未读红点、网页通知与移动端系统推送

## 1. 本期目标

1. 用户登录后，无论停留在首页、游戏、订单、钱包还是个人中心，收到客服新消息时都能及时感知。
2. 站内在线状态下播放一次消息提示音，并在底部 `Chat` 导航显示微信式未读红点。
3. 用户进入对应客服会话并成功标记已读后，红点立即消失；刷新页面和重新登录后状态仍与后端一致。
4. 支持手机端显示泛化通知文案：`您收到了一条消息`，点击后进入对应客服会话。
5. 区分站内 HTML 提示、浏览器系统通知、PWA Web Push 和原生 App 推送，按运行环境使用对应能力。

## 2. 当前代码现状

- [x] 底部导航已有聊天未读角标组件，并能读取 `supportUnreadCount`。
- [x] 聊天页面已支持 WebSocket、新消息提示音、页面隐藏时尝试调用浏览器 Notification。
- [x] App-Plus 已有推送设备 token 注册和通知点击进入客服会话的基础代码。
- [ ] 用户停留在首页等非聊天页面时，没有全局客服消息 WebSocket 监听，因此红点和提示音不会实时更新。
- [ ] 当前浏览器通知依赖页面仍在运行，浏览器关闭、手机锁屏或进程被清理后不保证到达。
- [ ] PWA Service Worker 尚未实现 Push 订阅和 `push` 事件处理。
- [ ] 原生推送仍需接通 Tencent TIMPush、iOS APNs 和 Android 厂商通道，并完成真机验证。

## 3. 站内全局消息提醒

- [ ] 登录成功后在 App 全局层建立用户专属客服会话监听，不依赖聊天页面是否打开。
- [ ] 页面切换时复用同一连接，避免首页与聊天页重复建立连接、重复播放声音或重复累计未读。
- [ ] 收到客服人工消息时更新全局未读状态，并在所有带底部导航的页面立即显示红点。
- [ ] 默认使用无数字的小红点；未读数量仍保留在状态和后端，后续可切换为数字角标。
- [ ] 只对客服发给用户的新消息提醒；用户自己的消息、系统同步事件、已读回执和重复消息不提醒。
- [ ] 当前正打开聊天且页面可见时，可不显示红点或立即清除；其他页面必须显示。
- [ ] 新消息播放一次提示音，短时间内多条消息需要做合并/节流，避免连续轰炸。
- [ ] 处理浏览器自动播放限制：用户首次点击页面后解锁音频；未解锁时红点仍必须正常工作。
- [ ] WebSocket 断线重连后从后端同步未读数，防止漏消息或重复计数。
- [ ] 多设备登录时，各设备分别显示未读；任一设备标记已读后，其余在线设备同步清除。

## 4. 手机通知分层

### 4.1 手机网页正在前台

- [ ] 使用 uni-app/HTML 站内提示条或轻量弹层显示：`您收到了一条消息`。
- [ ] 同时播放提示音并更新底部聊天红点。
- [ ] 点击提示直接进入客服聊天页。

### 4.2 浏览器退到后台但页面仍存活

- [ ] 用户授权通知权限后，使用浏览器 Notification API 显示系统通知。
- [ ] 通知文案只显示 `您收到了一条消息`，不暴露卡密、金额、银行卡或订单信息。
- [ ] 点击通知唤醒页面并进入对应客服会话。
- [ ] 未授权或浏览器不支持时，保留站内红点，用户回到页面后可看到。

### 4.3 浏览器关闭、锁屏或网页进程被清理

- [ ] PWA 路线：增加 Web Push 订阅、服务端订阅保存、VAPID 配置和 Service Worker `push`/`notificationclick` 处理。
- [ ] iPhone PWA 需要用户先“添加到主屏幕”并主动允许通知；普通 Safari 网页不保证关闭后推送。
- [ ] Android 浏览器需按 Chrome/PWA Web Push 真机验证，并考虑部分厂商后台限制。

### 4.3.1 现有 `/ios` 安装方式

- [x] 当前 `/install/xcard.mobileios` 会安装 Apple 配置描述文件中的全屏 Xcard Web Clip，入口指向 `https://stonetradex.com`；它不是原生 `.ipa` App。
- [ ] Web Clip 打开并保持运行时，接入全局消息监听后应支持站内红点、提示音和页面内消息提示。
- [ ] Web Clip 退到后台、锁屏或关闭后需要依赖 PWA Web Push，不能依赖普通 WebSocket 或 HTML 弹层。
- [ ] 在已安装的 Xcard 中提供用户主动点击的“开启消息通知”入口，用于申请 iOS 通知权限并创建 Push 订阅。
- [ ] 使用 iOS 16.4 及以上真机验证现有描述文件安装的 Web Clip 是否获得完整 Push API 能力。
- [ ] 安装页同时保留 Safari“添加到主屏幕”的标准 PWA 安装说明；若描述文件 Web Clip 的 Push API 受限，以标准 PWA 安装方式作为通知方案。
- [ ] 验证 Xcard 前台、后台、锁屏和关闭状态的通知到达，以及点击通知进入正确客服会话。

### 4.4 原生 App/安装包

- [ ] 接通 Tencent TIMPush 或统一推送服务。
- [ ] iOS 配置 APNs Auth Key/证书、Bundle ID 和生产推送环境。
- [ ] Android 按目标市场配置 FCM 或华为、小米、OPPO、vivo、荣耀等厂商通道。
- [ ] App 前台、后台、锁屏和杀进程状态均显示系统原生通知。
- [ ] 点击通知进入正确账号的客服会话；退出登录后注销旧设备 token。

## 5. 推荐实施顺序

1. P0：完成全局 WebSocket、全站红点、提示音、已读同步和断线补偿。
2. P1：完成 H5 页面内提示及页面仍存活时的浏览器系统通知。
3. P1：若主要以安装包交付，优先完成原生 App 推送；这是手机后台和杀进程场景最稳定的方案。
4. P2：若主要以“添加到主屏幕”的网站交付，再补充 PWA Web Push。

## 6. 验收场景

- [ ] 用户停留在首页，客服发消息后播放提示音，`Chat` 导航立即出现红点。
- [ ] 用户停留在游戏、订单、钱包或个人中心，表现与首页一致。
- [ ] 用户进入客服会话并成功标记已读，红点立即消失。
- [ ] 页面刷新、断网重连和重新登录后，未读状态与后端一致。
- [ ] 连续收到多条消息不会重复计数、重复弹窗或高频播放声音。
- [ ] H5 页面隐藏且已授权通知时，显示 `您收到了一条消息`。
- [ ] 通知点击后进入正确客服会话。
- [ ] iOS/Android 分别验证前台、后台、锁屏、杀进程和多设备登录。
- [ ] 通知正文不出现卡密、金额、银行卡和订单敏感信息。

## 7. 本期产品结论

1. 站内首页可见的红点和提示音属于 uni-app/H5 前端功能，可以直接在现有项目内完成。
2. 网页前台出现的提示条属于 HTML/uni-app UI。
3. 浏览器退到后台时显示在手机通知栏的弹窗属于浏览器系统通知，不是页面 HTML，但需要用户授权且页面存活情况会影响可靠性。
4. 浏览器关闭、锁屏或杀进程后仍要稳定收到通知，应使用原生 App 推送；PWA Web Push 可以作为网页安装版补充方案。
5. 现有 `/ios` 安装属于 Web Clip/PWA 路线；完成 Web Push 后可承担 iPhone 系统通知，但必须经过通知授权和真机能力验证。
